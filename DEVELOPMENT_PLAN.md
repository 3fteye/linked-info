# 关联信息管理应用开发计划

## 1. 产品定义

这是一个本地安装、手动录入的通用信息管理应用。核心能力不是管理某一种业务对象，而是：

1. 保存任意类型的信息。
2. 由用户定义信息类型及其字段。
3. 在任意两条信息之间建立有名称、有方向的关系。
4. 保存信息的来源、保存原因、当时场景和后续结果。
5. 通过字段、全文内容和关系查询信息。

账号、服务、优惠权益、脚本、网页和项目都只是用户可以创建的信息类型示例，应用不得为这些示例写死专用业务逻辑。

## 2. 当前范围

首个可用版本包含：

- 信息类型的创建、修改、排序和停用。
- 自定义字段的创建、校验和排序。
- 使用纯文本正文录入和查看信息。
- 信息记录的创建、查看、修改、归档和删除。
- 关系类型的创建，以及信息之间的双向可见关系。
- 来源、保存原因、场景上下文、后续结果和时间记录。
- 按类型、字段、状态和关系进行搜索、筛选和排序。
- 中立格式的数据导出、导入和备份恢复。
- 后端连接配置与后端实现切换。
- Windows 桌面安装包；代码保持 macOS 和 Linux 可移植。

首个版本不包含：

- 自动监控电脑活动。
- AI 自动分类或自动建立关系。
- 富文本编辑、Markdown 渲染和附件管理。
- 多人协作和复杂权限系统。
- 在应用数据库中保存账号密码、恢复码或 API 密钥。
- 通过浏览器直接使用的网页版产品。

## 3. 技术选择

### 3.1 桌面应用

- 桌面框架：Tauri 2
- 核心语言：Rust
- 界面：React + TypeScript
- Windows 渲染：WebView2
- macOS 渲染：WKWebView
- Linux 渲染：WebKitGTK

Tauri 负责桌面窗口、安装包、系统能力和安全边界。界面使用系统自带 WebView 渲染，不部署为网站，也不要求用户通过浏览器访问。

### 3.2 后端与存储

- 领域模型和业务规则：纯 Rust crate
- 应用用例：纯 Rust crate
- 后端协议：与供应商无关的 HTTP/JSON API
- 接口描述：由 Rust 类型生成 OpenAPI，前端从契约生成 TypeScript 客户端
- 首个远程后端：Cloudflare Worker
- 首个远程数据库适配器：Cloudflare D1
- 测试适配器：内存实现

Cloudflare 代码只允许存在于 Worker 入口和 Cloudflare 存储适配器中。领域层、应用层和桌面前端不得依赖 D1、Wrangler 或 Cloudflare SDK。

## 4. 架构边界

```text
React 桌面界面
        |
        | 只依赖生成的 API 客户端
        v
后端协议与数据契约
        |
        v
Rust 应用用例与领域模型
        |
        | 只依赖存储接口
        v
存储适配器
  |-- 内存适配器（测试）
  |-- Cloudflare D1/R2（首个实际后端）
  |-- SQLite/PostgreSQL 等其他实现（可独立加入）
```

替换数据库或部署平台时，只新增或替换适配器。前端交互、领域模型和已有数据契约不随供应商改变。

## 5. 核心模型

### 信息类型

描述一类信息的名称、说明、显示设置和字段定义。例如“脚本”可以拥有用途、来源地址、文件路径和运行说明等字段。

### 字段定义

字段包含稳定标识、名称、数据类型、必填约束、默认值、显示顺序和有效性规则。字段类型使用受控枚举，非法值在数据入口处立即拒绝。

### 信息记录

记录包含类型、标题、正文、自定义字段值、来源、保存原因、场景上下文、后续结果、创建时间和修改时间。

### 关系类型

关系类型包含正向名称和反向名称。例如正向“使用”、反向“被使用于”。方向和语义属于关系本身，不能退化为无含义的标签。

### 信息关系

关系连接两条信息，并保存关系类型、补充说明和创建时间。删除信息前必须明确处理其现有关系，禁止静默产生悬空引用。

## 6. 数据可移植性

应用必须提供独立于 Cloudflare 的完整导出格式，至少包含：

- 信息类型和字段定义。
- 所有信息记录及自定义字段值。
- 关系类型和信息关系。
- 导出格式版本和生成时间。

备份恢复测试属于发布验证的一部分。Cloudflare 的快照和 D1 导出只能作为额外保护，不能代替应用自身的完整导出。

## 7. 安全边界

- Cloudflare API Token 只用于开发和部署，不得进入桌面安装包。
- 桌面应用访问远程后端时使用应用级身份认证，不使用 Cloudflare 账户级令牌。
- 密码、恢复码和高权限密钥交给专用密码管理器保存；应用只保存引用和说明。
- 前端不得直接执行 SQL，也不得直接访问 D1 或 R2。
- 所有外部输入在 API 边界完成类型和约束校验，内部用例只接收有效数据。

## 8. 代码组织

```text
apps/
  desktop/                 Tauri 2 + React 桌面应用
  cloudflare-worker/       Cloudflare Worker 入口
crates/
  domain/                  信息、类型、字段和关系模型
  application/             用例和业务规则
  contracts/               API 请求、响应和 OpenAPI 契约
  storage-port/            存储接口
  storage-memory/          测试用内存适配器
  storage-d1/              Cloudflare D1 适配器
docs/
  architecture/            架构决策和数据格式说明
```

## 9. 实施阶段

### 阶段一：工程骨架

- 建立 Git 仓库和 Rust workspace。
- 创建 Tauri 2 桌面应用与 Cloudflare Worker 工程。
- 建立统一格式化、静态检查和测试命令。
- 建立前后端契约生成流程。

完成标准：空白桌面应用可以在 Windows 启动，Worker 可在本地开发环境响应健康检查，全部检查命令通过。

### 阶段二：领域模型

- 实现信息类型、字段定义、信息记录、关系类型和信息关系。
- 实现字段值校验、关系完整性规则和错误类型。
- 使用内存适配器完成核心用例测试。

完成标准：核心行为不依赖 Cloudflare，领域和应用测试全部通过。

### 阶段三：Cloudflare 后端

- 实现 D1 数据结构、迁移和存储适配器。
- 实现应用级身份认证和 API 边界校验。
- 验证完整数据导出。

完成标准：同一套应用测试可针对内存适配器和 Cloudflare 测试环境运行，行为一致。

### 阶段四：桌面工作流

- 实现信息类型和字段管理界面。
- 实现信息录入、编辑、归档和删除。
- 实现关系创建和正反向关系浏览。
- 实现搜索、筛选和排序。

完成标准：用户可以完全通过桌面界面创建自定义类型，录入信息并查询关联信息。

### 阶段五：备份与发布

- 实现完整导出、导入和恢复验证。
- 生成 Windows 安装包。
- 在 macOS 和 Linux CI 环境完成编译检查。
- 验证升级后数据迁移和旧备份兼容性。

完成标准：新安装环境可以从导出包恢复全部信息和关系，Windows 安装包可完成核心工作流。

## 10. 当前开发环境

- Rust 工具链根目录：`D:\soft\toolchains\rust`
- Cargo 目录：`D:\soft\toolchains\rust\cargo`
- Rustup 目录：`D:\soft\toolchains\rust\rustup`
- 默认工具链：`stable-x86_64-pc-windows-msvc`
- Windows C++ 工具链：`D:\soft\toolchains\cpp\vs-buildtools-2022`
- Windows WebView2：已安装

工具链目录按语言划分。后续新增语言时使用 `D:\soft\toolchains\<language>`，不混入 Rust 目录。

## 11. 当前进度

阶段一工程骨架已于 2026-08-08 完成：

- 已建立 Git 仓库与统一 Rust workspace。
- 已建立 Tauri 2 + React + TypeScript 桌面应用，并在 Windows 实际启动。
- 已建立与供应商无关的领域层、应用层和异步存储接口。
- 已建立内存存储实现和核心行为测试。
- 已建立 Rust Cloudflare Worker，`/health` 在本地返回结构化 JSON。
- 已通过 Worker Wasm 编译和 Wrangler `deploy --dry-run`。
- 已通过 Rust 格式检查、Clippy、核心测试和前端生产构建。

当前尚未创建远程 D1 数据库或部署 Worker。下一阶段从领域约束和内存实现继续，不让 Cloudflare 类型进入领域层或桌面界面。

## 12. 编译与持续集成策略

- 日常提交由 GitHub Actions 执行 Rust 格式检查、Clippy、核心测试、前端构建、Worker Wasm 检查和 Windows 桌面检查。
- Windows、macOS 和 Linux 安装包使用手动触发的 GitHub Actions 工作流生成，避免每次提交都消耗完整打包时间。
- 默认不在本机执行 Cargo、Worker 或 Tauri 编译。只有调试必须依赖本机窗口或用户明确要求时才进行本地编译。
- 本地仍保留 Rust、Node.js 和 pnpm 工具链，用于编辑器类型提示、依赖维护和必要的交互调试。
