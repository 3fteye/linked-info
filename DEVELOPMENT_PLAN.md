# 关联信息管理应用开发计划

> 本文档记录截至 2026-08-09 已确认的产品决定、技术约束和实施顺序。未确认的设计集中列在“待讨论事项”，不按推测实现。

## 1. 产品定义

这是一个本地安装、以手动录入为主的通用关联信息管理应用。它解决的核心问题是：信息保存后，来源、原因、当时场景以及与其他信息的联系容易丢失，导致以后无法理解当初为什么保存、从哪里得到、用于什么事情。

应用管理的是“信息及信息之间的关联”，而不是某一种固定业务对象。账号、服务、优惠码、脚本、网页和项目只是信息示例，不得为这些示例写死专用模型或流程。

首版遵循以下原则：

- 录入以用户手动填写为主，不自动监控电脑活动。
- 节点保持极简，只让用户填写名称和纯文本内容。
- 来源、原因和场景不做成固定字段，而由普通节点及节点之间的引用共同表达。
- 画布是可替换的展示方式，不是领域模型或存储格式。
- Cloudflare 是首个后端选项，不是产品不可替换的基础。

## 2. 已确认的核心模型

### 2.1 正式节点 `Node`

```text
Node
  id       系统生成的稳定内部 ID
  name     用户填写的名称，必填且唯一
  content  用户填写的纯文本内容，可空
```

约束与语义：

- 名称相当于标签名，是用户搜索、选择和识别节点的入口。
- 在当前资料库中，正式节点名称必须唯一。创建或改名产生冲突时不能保存为正式节点，应引导用户选择已有节点或修改名称。
- 判断同名时先移除首尾空格并忽略大小写；名称显示保留用户输入的大小写。首版不合并连续空格，也不做额外 Unicode 兼容形式折叠。
- 内部 ID 与名称职责不同：ID 用于持久化身份和引用，名称用于人类理解。改名不得破坏已有引用。
- 名称不能作为数据库主键或引用外键。
- 内容可以为空，首版只保存纯文本，不引入富文本或 Markdown 编辑逻辑。
- 节点没有固定“类型”。任何节点既可以承载信息，也可以被其他节点引用并充当标签。

### 2.2 引用 `Reference`

```text
Reference
  source_node_id
  target_node_id
```

引用是有方向的，但首版不额外保存关系类型、关系名称或关系说明。

`A -> OpenAI` 表示节点 A 引用了节点 OpenAI。筛选 `OpenAI` 时，展示直接引用它的节点。由此，来源、保存原因、当时场景、所属服务等信息都可以由多个普通节点的组合表达，而不需要固定字段。

### 2.3 草稿 `Draft`

正式节点必须有名称，但尚未形成正式节点的输入不能因此丢失。草稿与正式节点分离：

- 草稿可以暂时没有名称。
- 编辑内容需要自动保存到本地。
- 应用误关或重启后可以恢复未完成草稿。
- 校验不通过时保留草稿，不能清空用户已经输入的内容。
- 只有满足正式节点约束后，草稿才能提交为节点。

草稿是录入保护机制，不参与正式节点搜索、筛选或引用。

### 2.4 画布布局 `CanvasLayout`

节点内容、引用关系和视图布局必须分开保存。布局至少需要表达：

```text
CanvasLayout
  canvas_id
  node_id
  x
  y
  width
  collapsed
```

这些字段属于画布视图状态，不属于节点。移动、折叠或更换视图不得修改节点及引用；未来增加列表等展示方式时，也不得迁移领域数据。

## 3. 已确认的交互方向

### 3.1 画布

- 主要展示方式采用类似 ComfyUI 的大平面，节点可自由拖动和连接。
- 画布节点保持紧凑，主要显示名称和内容摘要。
- 长内容在独立的侧边编辑区域查看和编辑，避免节点因正文过长而失去可浏览性。
- 画布实现应通过视图接口读取节点和引用，不能把坐标写入节点模型。
- 画布只是首个视图；之后可以增加或切换其他视图。

### 3.2 新建与编辑

- 新建输入先进入本地草稿自动保存流程。
- 提交正式节点时校验名称必填和名称唯一。
- 节点编辑仅包含名称、内容和引用选择，不引入信息类型或自定义字段编辑器。
- 选择被引用节点时展示其唯一名称；实际保存的是内部 ID。

### 3.3 搜索与筛选

- 默认按节点名称搜索。
- 选择一个节点作为筛选条件时，展示直接引用该节点的节点。
- 一个节点可以同时引用多个节点，以标签组合还原来源、原因和场景。

## 4. 上下文记录机制

已经确认：

- 来源、为什么保存、从哪里得到和当时场景不设计为固定字段。
- 用户通过引用相关节点，并在当前节点内容中补充说明来记录上下文。
- 需要一个可开关的上下文约束功能；关闭时不强迫用户补全上下文。
- 无论约束是否满足，都不能丢失已输入内容，应继续保存在草稿中。

上下文开关的具体行为尚未确认，见第 12 节。此前提出的“锁定当前上下文节点并让新节点自动引用”仍只是候选方案，不得当作既定需求实现。

## 5. 首个可用版本范围

包含：

- 正式节点的创建、查看、修改和删除。
- 名称必填及资料库内名称唯一约束。
- 纯文本内容录入，内容可空。
- 本地草稿自动保存和重启恢复。
- 节点之间引用的创建、移除和浏览。
- 默认名称搜索，以及按被引用节点筛选直接引用者。
- 可拖动、可连接的无限画布视图。
- 与节点分离的画布布局持久化。
- 中立格式的数据导出、导入和恢复验证。
- 后端连接配置及后端实现可替换。
- Windows 桌面安装包；代码保持 macOS 和 Linux 可移植。

首版不包含：

- 信息类型、自定义字段和固定业务表单。
- 独立的标签模型；节点本身就是可引用的标签。
- 关系类型、关系名称和关系说明。
- 自动监控、自动采集、AI 自动分类或 AI 自动建立引用。
- 富文本编辑、Markdown 渲染和附件管理。
- 多人协作和复杂权限系统。
- 通过浏览器直接使用的网页版产品。
- 在应用数据库中保存账号密码、恢复码或高权限 API 密钥；这类秘密仍交给专用密码管理器，节点只记录非秘密说明或引用。

## 6. 技术选择

### 6.1 桌面应用

- 桌面框架：Tauri 2
- 核心语言：Rust
- 界面：React + TypeScript
- 多语言：i18next + react-i18next；界面文字使用独立语言资源，首批提供简体中文和英文
- Windows 渲染：WebView2
- macOS 渲染：WKWebView
- Linux 渲染：WebKitGTK

Tauri 负责桌面窗口、安装包、系统能力和安全边界。界面由跨平台 Web 技术实现，但产品是本地桌面应用，不部署成供用户直接访问的网站。

### 6.2 后端与存储

- 领域模型和业务规则：纯 Rust crate
- 应用用例：纯 Rust crate
- 后端协议：与供应商无关的 HTTP/JSON API
- 接口描述：由 Rust 类型生成 OpenAPI，前端从契约生成 TypeScript 客户端
- 首个远程后端：Cloudflare Worker
- 首个远程数据库适配器：Cloudflare D1
- 测试适配器：内存实现

Cloudflare 代码只允许存在于 Worker 入口和 Cloudflare 存储适配器中。领域层、应用层和桌面前端不得依赖 D1、Wrangler 或 Cloudflare SDK。

## 7. 架构边界

```text
桌面视图（画布 / 将来的其他视图）
        |
        | 统一的前端数据与命令接口
        v
React 桌面应用层
        |
        | 供应商无关的 API 客户端与契约
        v
Rust 应用用例与领域模型
        |
        | 存储接口
        v
存储适配器
  |-- 内存适配器（测试）
  |-- Cloudflare Worker + D1（首个实际后端）
  |-- 其他后端或数据库（可独立加入）

本地草稿存储
  |-- 只服务于桌面录入恢复，不混入正式节点模型
```

有两条独立边界：

1. 更换画布为列表等视图，不改变节点、引用和后端数据契约。
2. 更换 Cloudflare 或数据库，只替换后端入口或存储适配器，不改变桌面交互和领域规则。

## 8. 数据可移植性与安全边界

应用必须提供独立于 Cloudflare 的完整导出格式，至少包含：

- 格式版本和生成时间。
- 全部正式节点，包括稳定内部 ID、唯一名称和内容。
- 全部引用的源节点 ID 与目标节点 ID。
- 与领域数据分离的画布布局。

备份恢复测试属于发布验证的一部分。Cloudflare 快照和 D1 导出只能作为额外保护，不能代替应用自身的完整导出。

安全约束：

- Cloudflare API Token 只用于开发和部署，不得进入桌面安装包。
- 桌面应用访问远程后端时使用应用级身份认证，不使用 Cloudflare 账户级令牌。
- 前端不得直接执行 SQL，也不得直接访问 D1。
- 所有外部输入在 API 边界完成类型和约束校验，内部用例只接收有效数据。

## 9. 代码组织

```text
apps/
  desktop/                 Tauri 2 + React 桌面应用及画布视图
  cloudflare-worker/       Cloudflare Worker 入口
crates/
  domain/                  Node 与 Reference 领域模型及约束
  application/             节点、引用、搜索和筛选用例
  contracts/               API 请求、响应和 OpenAPI 契约
  storage-port/            正式数据存储接口
  storage-memory/          测试用内存适配器
  storage-d1/              Cloudflare D1 适配器
```

草稿恢复和画布视图状态属于桌面端能力；如果后续确实需要跨设备同步，再单独扩展契约，首版不预先增加同步模型。

## 10. 实施阶段

### 阶段一：修正领域骨架

- 删除现有 `InformationType`、`FieldDefinition`、`InformationContext` 和 `RelationType` 方向的领域设计。
- 将核心模型收敛为 `Node` 和 `Reference`，并将画布布局排除在领域节点之外。
- 实现名称必填、名称唯一、稳定 ID 引用和引用完整性规则。
- 同步修改应用用例、存储接口、内存适配器和测试。

完成标准：领域代码只表达已确认的节点与引用模型，测试覆盖名称约束、改名后引用稳定以及按目标节点查询直接引用者。

### 阶段二：后端契约与 Cloudflare 适配器

- 定义供应商无关的节点、引用、搜索和筛选 API。
- 实现 D1 数据结构、迁移和存储适配器。
- 在 API 边界实现名称与引用约束校验。
- 保证 Cloudflare 类型不进入领域层或桌面前端。

完成标准：同一套应用测试可针对内存适配器和 Cloudflare 测试环境运行，行为一致。

### 阶段三：桌面最小工作流

- 实现本地草稿自动保存、恢复和提交。
- 实现节点新建、编辑、删除和引用选择。
- 实现默认名称搜索和按目标节点筛选直接引用者。
- 实现侧边内容编辑区域。

完成标准：用户可以从草稿创建正式节点、建立引用，并通过名称或被引用节点重新找到信息；异常关闭不丢失未提交输入。

### 阶段四：画布视图

- 实现可平移、缩放、拖动节点和创建引用的画布。
- 画布节点显示名称和内容摘要。
- 独立持久化坐标、宽度和折叠状态。
- 验证更改布局不会修改节点或引用数据。

完成标准：核心录入和查询工作可以在画布完成，布局数据与领域数据边界清晰。

### 阶段五：备份与发布

- 实现完整导出、导入和恢复验证。
- 生成 Windows 安装包。
- 在 macOS 和 Linux CI 环境完成编译检查和安装包构建。
- 验证升级后的数据迁移和旧备份兼容性。

完成标准：新安装环境可以从导出包恢复全部节点、引用和布局，Windows 安装包可完成核心工作流。

## 11. 当前工程状态与开发约束

现有工程骨架已经具备：

- Git 仓库与统一 Rust workspace。
- Tauri 2 + React + TypeScript 桌面应用骨架。
- 基于独立语言资源的简体中文、英文界面及语言选择持久化。
- 分层的领域层、应用层和异步存储接口。
- 内存存储实现和 Rust Cloudflare Worker 骨架。
- GitHub Actions 的核心 Rust、Worker Wasm、前端和 Windows Tauri 检查。
- 手动触发的 Windows、macOS 和 Linux 安装包构建工作流。

领域代码已经替换为 `Node` 与 `Reference` 模型；名称非空由领域类型保证，名称唯一由存储保存边界原子保证。内存适配器和应用用例覆盖名称搜索、直接引用者筛选以及改名后引用稳定。旧的“信息类型、自定义字段、固定上下文、关系类型”模型已移除。

开发环境：

- Rust 工具链根目录：`D:\soft\toolchains\rust`
- Cargo 目录：`D:\soft\toolchains\rust\cargo`
- Rustup 目录：`D:\soft\toolchains\rust\rustup`
- Windows C++ 工具链：`D:\soft\toolchains\cpp\vs-buildtools-2022`
- Windows WebView2：已安装

编译与验证策略：

- 默认不在本机执行 Cargo、Worker 或 Tauri 编译。
- 日常检查和编译交给 GitHub Actions。
- Windows、macOS 和 Linux 安装包使用手动触发的 GitHub Actions 工作流生成。
- 只有必须依赖本机窗口的交互调试或用户明确要求时，才进行本地编译。

## 12. 待讨论事项

以下事项尚未获得明确决定，不能根据旧草稿或助手建议擅自补全：

1. **上下文开关的具体规则**：开启后要求引用几个节点、是否由用户配置一组当前上下文节点、是否自动附加引用，以及何时允许草稿转为正式节点。
2. **引用边界规则**：是否允许节点引用自己、是否允许相同方向的重复引用、删除被引用节点时采用阻止删除还是明确确认后连带移除引用。
3. **画布集合语义**：`canvas_id` 是只为未来视图隔离预留，还是首版就支持多个独立画布；同一个节点是否能出现在多个画布。
4. **内容搜索**：首版已经确认默认搜索名称，但是否提供显式的内容全文搜索尚未确认。
5. **用户忘记的功能**：目前没有可恢复的原始描述，不猜测，等用户重新想起后再补充。

上述事项不妨碍先完成阶段一中已确认的模型替换；涉及对应行为时必须先形成产品决定。
