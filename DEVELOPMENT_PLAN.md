# 关联信息管理应用开发计划

> 本文档记录截至 2026-08-12 已确认的产品决定、技术约束和实施顺序。未确认的设计集中列在“待讨论事项”，不按推测实现。

## 1. 产品定义

这是一个本地安装、以手动录入为主的通用关联信息管理应用。它解决的核心问题是：信息保存后，来源、原因、当时场景以及与其他信息的联系容易丢失，导致以后无法理解当初为什么保存、从哪里得到、用于什么事情。

应用管理的是“信息及信息之间的关联”，而不是某一种固定业务对象。账号、服务、优惠码、脚本、网页和项目只是信息示例，不得为这些示例写死专用模型或流程。

首版遵循以下原则：

- 录入以用户手动填写为主，不自动监控电脑活动。
- 节点保持极简，只让用户填写名称和纯文本内容。
- 来源、原因和场景不做成固定字段，而由普通节点及节点之间的引用共同表达。
- 画布是可替换的展示方式，不是领域模型或存储格式。
- Cloudflare 是首个后端选项，不是产品不可替换的基础。

## 2. 已确认的核心模型

### 2.1 正式节点 `Node`

```text
Node
  id       系统生成的稳定内部 ID
  name     用户填写的名称，可空；非空时唯一
  content  用户填写的纯文本内容，可空
```

约束与语义：

- 节点一经创建就具有稳定内部 ID；名称和内容都可以为空，空节点仍可被引用并参与筛选。
- 非空名称相当于标签名，是用户搜索、选择和识别节点的主要入口。空名称节点在界面上使用本地化的“未命名”占位文字，但占位文字不写入数据。
- 在当前资料库中，非空名称必须唯一；多个空名称节点允许同时存在。
- 判断同名时先移除首尾空格并忽略大小写；名称显示保留用户输入的大小写。首版不合并连续空格，也不做额外 Unicode 兼容形式折叠。
- 内部 ID 与名称职责不同：ID 用于持久化身份和引用，名称用于人类理解。改名不得破坏已有引用。
- 名称不能作为数据库主键或引用外键。
- 内容可以为空，首版只保存纯文本，不引入富文本或 Markdown 编辑逻辑。
- 节点没有固定“类型”。任何节点既可以承载信息，也可以被其他节点引用并充当标签。
- 不新增“关联节点”或“服务记录”等代码实体。需要记录“某个账号在某个服务上的具体情况”时，创建一个普通节点保存详细文字，并让它同时引用账号节点和服务节点。
- 共享信息写在被多个记录引用的节点中；只属于一组参与方的信息写在同时引用这些参与方的普通记录节点中。例如 OpenAI 的通用说明属于 `OpenAI` 节点，而账号 A 的套餐、费用和封禁状态属于同时引用 `账号 A` 与 `OpenAI` 的记录节点。
- 具体且不需要跨记录筛选的信息优先直接写在内容中；只有需要反复筛选、统一归类或避免同义写法分裂的信息，才提升为可复用的引用节点。

### 2.2 引用 `Reference`

```text
Reference
  source_node_id
  target_node_id
```

引用是有方向的，但首版不额外保存关系类型、关系名称或关系说明。

`A -> OpenAI` 表示节点 A 引用了节点 OpenAI。筛选 `OpenAI` 时，展示直接引用它的节点。由此，来源、保存原因、当时场景、所属服务等信息都可以由多个普通节点的组合表达，而不需要固定字段。

### 2.3 本地编辑与同步状态

节点在画布上创建时立即进入本地工作区，不需要先通过表单提交。编辑状态遵循本地优先：

- 输入先更新内存，并在停止输入约 300 毫秒后保存整个本地工作区。
- 编辑中的节点必须由节点编辑器直接持有当前输入状态；画布和外层工作区同步不得重置文本选区或滚动位置，`Enter`、`Shift+Enter` 和 `Backspace` 必须按普通多行文本输入行为工作。
- 创建节点、离开整个节点和应用关闭前必须立即刷新本地保存，不能等待防抖计时器。
- 从名称输入框切换到同一节点的内容输入框仍属于节点内部编辑，不触发远端提交。
- 焦点离开整个节点时，名称和内容作为一个节点整体产生一次远端提交意图；不得按字符或按字段请求远端。
- 远端失败不得回滚或清空本地数据，而应保留节点并标记为待同步。
- 节点位置在拖动结束后独立保存；引用在连线建立或移除后独立保存，不能混入节点文本提交。

桌面端尚未接入远端 API 时，只执行本地保存；不得把没有发生的远端同步显示为成功。

### 2.4 画布布局 `CanvasLayout`

节点内容、引用关系和视图布局必须分开保存。布局至少需要表达：

```text
CanvasLayout
  canvas_id
  node_id
  x
  y
  width
  collapsed
  stack_order
```

这些字段属于画布视图状态，不属于节点。移动、折叠或更换视图不得修改节点及引用；未来增加列表等展示方式时，也不得迁移领域数据。

节点遮挡顺序同样属于画布视图状态。当前工作区快照使用 `layout` 数组从后到前表达堆叠顺序：越靠后的布局项越靠近用户。新建、单击、双击或右键操作时，将对应布局项移动到数组末尾。开始拖动时只在 React Flow 的局部状态中临时抬升节点，不能为了置顶而重建并保存整个工作区；拖动结束后再把最终位置与层级作为一次布局操作写入数组末尾并持久化。React Flow 自带的临时选中抬升必须关闭，避免“编辑焦点”和“画布选中”使用两套互相冲突的层级规则。

## 3. 已确认的交互方向

### 3.1 画布

- 主要展示方式采用类似 ComfyUI 的大平面，节点可自由拖动和连接。
- 画布使用随视口平移、缩放的清晰线性网格，使用户能够直接感知画布移动。
- 画布节点保持紧凑，直接在节点内部编辑名称和纯文本内容；拖动区域与文本输入区域必须分离。
- 节点失去焦点后恢复为名称和内容摘要视图，避免长内容长期占据过大画布空间。
- 节点采用“最后交互者在最上方”的稳定遮挡规则。新建、单击、双击、右键和开始拖动都会将节点置顶；当前输入或拖动的节点不得被旧的选中节点遮挡，置顶顺序在重启后保持。
- 画布空白处左键或中键拖动用于平移；按住 `Shift` 后在空白处左键拖动用于框选；`Ctrl`/`Shift` 加点击用于追加选择。拖动任一已选节点时，全部选中节点共同移动并以一次布局操作保存。
- 画布未在文字输入状态时，`Ctrl+A` 选择当前可见节点；`Delete`/`Backspace` 对所选节点统一弹出确认后批量删除，删除节点时同时删除相连引用。
- 画布保存最后一次平移和缩放形成的视口；旧工作区没有视口数据时首次自动适应全部节点，之后使用已保存视口恢复。
- 画布提供最多 100 步的内存级撤销/重做，支持工具栏以及 `Ctrl+Z`、`Ctrl+Y`/`Ctrl+Shift+Z`，覆盖节点创建、整节点编辑、节点删除、节点移动和引用增删；文字输入框获得焦点时仍使用系统原生文字撤销，画布平移、缩放、筛选和节点置顶不进入操作历史。
- 从节点的引用输出端直接拖线：落到目标节点时建立引用，落到画布空白处时在落点优先打开现有节点搜索，不占用 `Shift`；`Shift` 继续只用于画布框选和追加选择。候选项排除当前节点、已经引用的节点和已在本次连续操作中选过的节点。
- 连续引用搜索默认高亮第一个候选项。存在现有候选时，`Space` 将高亮项立即加入引用、清空搜索词并保持搜索框，便于继续选择；`Enter` 将高亮项加入引用后关闭搜索框；`Escape` 关闭且不追加当前候选。方向键用于移动高亮项，鼠标选择现有候选等同于 `Space`，搜索会继续保留。搜索无结果、搜索词非空且不与现有非空名称冲突时，候选区改为“新建并引用”；此时 `Space` 不执行创建，只有 `Enter` 或显式鼠标点击才新建并引用，随后立即关闭搜索，避免搜索输入与新节点后续编辑形成竞争状态。
- 画布实现应通过视图接口读取节点和引用，不能把坐标写入节点模型。
- 画布只是首个视图；之后可以增加或切换其他视图。

### 3.2 新建与编辑

- 用户选择“新建节点”后，节点立即出现在触发位置并进入节点内编辑，不先打开侧边属性面板或模态表单。
- 名称和内容都可为空；非空名称在规范化后仍需满足唯一约束。
- 新建节点自动聚焦名称输入框；用户可以直接切换到内容输入框继续填写。
- 节点整体失去焦点时结束行内编辑、立即刷新本地保存，并以完整节点为最小单位准备远端提交。
- 节点编辑仅包含名称、内容和引用选择，不引入信息类型或自定义字段编辑器。
- 选择被引用节点时，已命名节点显示名称，未命名节点显示“未命名”和内容摘要；实际保存的始终是内部 ID。

### 3.3 搜索与筛选

- 当前桌面搜索框只按节点名称搜索，不搜索内容。内容全文搜索仍是独立的候选功能，不能把当前名称搜索描述成已经覆盖正文。
- 提供“仅未命名”系统筛选，用于集中整理名称为空的节点；填写名称后节点自动退出该筛选结果。
- 选择一个节点作为筛选条件时，展示直接引用该节点的节点。
- 可以同时选择多个节点作为筛选条件。多个引用条件采用 AND 语义：结果节点必须直接引用全部所选节点。
- 画布筛选时保留所选条件节点本身，以便继续看到结果节点到条件节点的连线；结果数量只统计满足条件的引用者，不把条件节点计入结果。
- 节点内部展示它直接引用的全部目标。已命名目标显示名称；未命名目标显示本地化的“未命名”和内容摘要。
- 一个节点可以同时引用多个节点，以标签组合还原来源、原因和场景。

### 3.4 智能引用第一层

智能引用的第一层使用文本向量寻找相似信息，再利用现有引用图传播这些相似信息已经使用的引用。向量相似节点只作为分类证据，不直接成为自动引用候选；该层不判断事实真伪，也不把推荐分解释为“分类正确率”。

已确认规则：

- 向量能力通过供应商无关的 `EmbeddingProvider` 接口接入，至少提供“本地模型”和“远端接口”两种适配器。画布、节点模型和引用模型不依赖具体模型、云厂商或 HTTP 格式。
- 本地方式优先面向普通电脑 CPU，首个适配器使用 ONNX 文本向量模型；模型文件首次使用时下载到当前设备的应用缓存目录，不进入工作区、导出包或 Git 仓库。缓存被系统或用户清理后允许重新下载。
- 本地模型只展示经过应用验证的精简目录，不直接暴露 FastEmbed 的全部模型枚举。首批提供三种有明确取舍的选择：中文优先的 BGE Small Chinese v1.5（约 91 MiB、512 维、MIT）、英文轻量的量化 all-MiniLM-L6-v2（约 23 MiB、384 维、Apache-2.0），以及中英和多语言增强的 multilingual-e5-small（约 465 MiB、384 维、MIT）。新安装默认使用中文优先模型；已经保存的模型选择继续保留。
- 模型说明必须展示适用语言、近似下载量、向量维度、CPU/内存相对负担、主要适用场景、局限、许可证、实际下载仓库和固定版本。下载量来自应用固定版本的必需文件总和，不得把参数量误写为下载量。
- 本地模型生命周期拆分为检查缓存、下载、校验、加载、推理、就绪、取消和失败。下载界面至少显示模型名、当前文件、文件序号、已下载/总字节、总百分比、速度和可用时的剩余时间；加载和推理不得继续显示成“正在下载”。
- 模型下载必须允许取消。取消只停止本次任务并保留可验证的部分文件供下次续传，不删除已完成文件，不把半成品标记为可用；同一时间只允许一个本地模型下载或加载任务。
- 本地模型使用固定仓库版本和已知文件大小；模型权重等大文件还需校验已登记的 SHA-256。全部必需文件完成后才能原子地发布为可加载缓存。模型缓存是设备本地派生文件，可重新下载，不进入工作区、导出包、远端同步或 Git。
- 远端方式使用可配置的嵌入接口地址和模型名。远端令牌属于秘密，只保存在当前应用会话中；在接入系统凭据库前，不写入工作区、浏览器存储、日志或导出包。
- 切换提供方式、远端地址或模型名时立即清除会话令牌，避免把旧提供者的凭据误发给新端点。
- 用户从某个节点明确触发一次分析。输入停止、自动保存或节点失焦都不自动调用模型，避免本地持续占用和远端逐字计费。
- 分析文本由节点名称和内容组成。名称与内容都为空的节点没有可计算文本，既不能作为分析源，也不进入候选结果。
- 向量检索覆盖除源节点外所有具有可计算文本的节点；结果按余弦相似度排序，并在界面中作为“相关信息”单独展示，不得直接建立引用。
- 推荐引用采用图传播：先选择具有出向引用的最相似记录作为种子，再只吸收与当前证据簇共享至少一个引用目标的相似记录，避免固定数量的其他主题记录凭数量污染推荐；最终候选是该证据簇引用的目标，而不是证据节点本身。源节点自己和已经存在的引用目标不再进入可新增候选。
- 推荐分由“证据簇对该引用目标的加权支持比例”和“是否有至少两条独立记录重复支持”共同组成，范围为 0 到 1，但不是概率。只有一条历史记录支持的候选最高为 0.5，默认不能触发 0.6 的自动引用阈值；界面必须展示支持该候选的相似节点数量，并允许查看独立的相关信息列表。
- 设置中分别提供“启用阈值自动引用”开关和推荐分阈值。开关默认关闭；开启后，一次明确分析会把推荐分大于或等于阈值的候选建立为普通 `Reference`。阈值以下的候选仍可由用户手动确认。
- 自动引用只做增量添加，不因重新分析、切换模型或提高阈值而自动删除现有引用。用户可以通过普通引用操作撤销或移除结果。
- 推荐分阈值同时与供应商、模型、模型版本和图传播算法版本绑定，不是跨配置通用参数。切换本地/远端方式、模型或推荐算法版本后必须关闭旧的阈值自动引用并提示重新校准；只有模型输入真正变化时才切换派生向量缓存命名空间，单纯调整图传播算法不得重复计算相同文本向量。
- 当前图传播证据簇至多包含 8 个节点；证据权重根据其与种子相似度的相对差距衰减，候选分数再除以全部证据权重，并按重复支持数量限制可靠度。具体常数和证据簇规则属于算法版本，修改时必须使旧阈值失效并通过实际资料库回放验证。
- 从未被任何历史记录引用、又没有可识别角色的全新节点无法仅靠无类型图判断其是否被设计为标签。第一层不再用“直接把任意相似节点当标签”的方式猜测；此类标签至少需要一次手动引用，或等待后续明确的候选资格设计。
- 长内容按有界分段计算并聚合最高相关度，避免静默只看开头；同时限制单节点分段数，防止极长脚本导致无限计算或远端费用失控。
- 派生向量、索引、分数和当前候选结果不是正式领域数据，不进入 `Node + Reference`、工作区快照或中立导出格式。模型最终建立的引用仍使用普通 `Reference`，不新增“智能引用”实体。
- “模型缓存”和“向量缓存”是两个独立概念：模型缓存保存下载的模型文件；向量缓存保存模型对节点文本的计算结果。删除向量缓存只会触发重新计算，不会删除节点、引用或模型文件。
- 向量缓存属于设备本地派生数据，不是领域数据。持久化缓存使用独立的 SQLite 文件，向量以紧凑的 `Float32 BLOB` 保存；该文件不得进入工作区、备份、导出、远端同步或 Git，也不得与正式节点数据库共用生命周期。
- 持久化向量缓存默认磁盘上限为 512 MiB。达到上限后按最近最少使用顺序回收旧条目；设置页必须显示实际占用，并提供只清空向量缓存的操作。清空失败必须保留明确错误，不得连带删除模型文件或正式数据。
- 运行时内存缓存使用有容量上限的 LRU，不再使用可无限增长的 `Map`。默认上限为 64 MiB，并允许以后按设备情况提高到 128 MiB；内存淘汰只影响访问速度，不删除磁盘缓存。
- 缓存键使用“模型配置指纹 + 文本角色 + 内容 SHA-256”，不得把完整文本复制进内存键或 SQLite 主键。缓存记录不保存节点名称、正文、远端令牌或其他正式数据；配置指纹至少区分提供方式、模型标识、固定版本和影响输入预处理的选项。
- 节点名称或内容变化时，只使该节点旧文本对应的运行时关联失效，其他节点的缓存继续可用。更换提供方式、模型或模型版本时切换缓存命名空间，不执行会阻塞界面的全库清空；旧命名空间随后按容量回收规则删除。
- 查询角色与文档角色必须参与缓存键。即使某些模型两种角色输入相同，也不得假定所有模型都能共用向量；例如带不同查询/文档前缀的模型必须分别缓存。
- 余弦相似度计算继续使用有界候选扫描；SQLite 只负责避免重复推理或重复远端请求，不在没有性能数据前引入向量数据库或近似最近邻索引。
- 第一层不接入重排模型或 LLM；第二层在独立契约和设置开关之后消费第一层结果，不得反向污染向量缓存或图传播算法。

### 3.5 智能引用第二层：本地 LLM 复核

LLM 第二层负责理解当前节点、已有引用、候选引用及少量历史使用示例，从已有节点中选择更符合语义的引用。它不扫描或修改完整工作区，不创建节点，不生成标签名称，也不删除已有引用。

已确认规则：

- LLM 合并进现有“智能引用”操作，通过独立开关启用，不增加第二个分析按钮。执行顺序固定为“向量检索 → 图传播与候选池 → LLM 复核 → 用户确认”。
- LLM 能选择的候选由两部分合并：图传播候选，以及至少已经被其他节点引用过一次的节点。候选按图传播证据和文本相似度有界排序；未被使用过的新节点仍由用户手动创建并至少引用一次后，才进入以后可学习的候选集合。
- 发送给 LLM 的上下文只包含当前节点的有界文本、已有引用摘要、最多 24 个候选，以及每个候选最多两条有界历史示例。正式节点 ID 替换为本次请求的临时候选编号，响应只允许返回这些编号。
- 第一版对外响应只有 `selected`、`uncertain` 和 `noMatch`，不生成自然语言理由或自报置信度。模型输出只包含 `selected` 和 `uncertain` 两组候选，`noMatch` 由可信的 Rust 边界根据两组是否均为空唯一推导。结构必须由 JSON Schema/语法约束，并在应用边界再次严格验证；未知编号、重复编号、交叉分组或结构冲突立即失败，不能静默建立引用。
- `selected` 和 `uncertain` 都只进入结果界面，由用户逐条确认。LLM 第一版没有自动建立引用权限；启用 LLM 复核时暂停原有推荐分阈值自动引用，关闭 LLM 时保留第一层既有行为。
- `noMatch` 表示当前候选池没有合适引用，只提示用户自行判断是否创建新节点；LLM 不得提出节点名称后直接创建实体。
- LLM 能力通过供应商无关的 `LlmProvider` 契约接入。本轮实现本地适配器；远端作为以后可选适配器保留在边界外，不实现隐私脱敏、秘密扫描或后台传输。
- 本地运行时采用固定版本的 `llama.cpp` CPU sidecar 与 GGUF 模型。Rust 负责启动、健康检查、请求、超时和应用退出时终止；sidecar 只监听 `127.0.0.1`、使用随机会话密钥、关闭 Web UI 和工具能力，React 不直接访问本地端口。
- 第一版本地模型使用官方 `Qwen/Qwen3-1.7B-GGUF` 的 Q8_0 文件，固定仓库提交、大小和 SHA-256，关闭思考模式，限制约 4096 tokens 上下文和 128 tokens 输出。模型下载支持状态检查、续传、详细进度、取消和校验，不进入工作区、导出、同步或 Git。
- CPU 推理线程必须保留系统余量并设有上限，不能占满全部逻辑核心；同一时间只运行一个本地 LLM 下载、加载或推理任务。首版不启用 CUDA、Vulkan 或其他 GPU 后端，但运行时边界允许以后按平台替换。
- LLM 进程只在首次复核时按需加载，应用退出时必须结束；禁用 LLM 时允许主动卸载。模型进程异常退出、加载失败、请求超时或响应无效都只产生明确错误，第一层结果和正式引用保持不变。

## 4. 上下文记录机制

已经确认：

- 来源、为什么保存、从哪里得到和当时场景不设计为固定字段。
- 用户通过引用相关节点，并在当前节点内容中补充说明来记录上下文。
- 不实现单独的“当前上下文”开关、强制引用数量、自动附加上下文节点或额外录入提醒。这些机制只增加提醒和录入负担，没有提供现有普通引用无法表达的新能力。
- 应用不得因为用户没有填写来源或场景而阻止节点保存；上下文是否充分由用户自行判断。

## 5. 首个可用版本范围

包含：

- 正式节点的创建、查看、修改和删除。
- 名称和纯文本内容均可为空，非空名称在资料库内唯一。
- 直接在画布节点内录入名称和纯文本内容。
- 本地优先自动保存和重启恢复；远端按节点整体提交。
- 节点之间引用的创建、移除和浏览。
- 默认名称搜索，以及按被引用节点筛选直接引用者。
- 按空名称筛选未命名节点。
- 可拖动、可连接的无限画布视图。
- 与节点分离的画布布局持久化。
- 中立格式的数据导出、导入和恢复验证。
- 后端连接配置及后端实现可替换。
- 可替换的本地或远端文本向量适配器、候选结果和可选的阈值自动引用。
- 可选的本地 LLM 候选复核；LLM 只选择已有引用目标并由用户确认。
- Windows 桌面安装包；代码保持 macOS 和 Linux 可移植。

首版不包含：

- 信息类型、自定义字段和固定业务表单。
- 独立的标签模型；节点本身就是可引用的标签。
- 复制、粘贴或克隆节点。当前产品以人工录入和非空名称唯一为约束，复制节点容易制造重复名称及错误引用，不属于基础操作闭环。
- 关系类型、关系名称和关系说明。
- 自动监控、自动采集、后台批量分类，以及不经用户明确触发的 AI 自动修改。
- 富文本编辑、Markdown 渲染和附件管理。
- 多人协作和复杂权限系统。
- 通过浏览器直接使用的网页版产品。

后续已经确认的方向：

- 应用允许保存密码、访问令牌、双因素恢复信息等秘密，但只有启用密码保护的加密工作区后才能把它们视为安全存储。当前明文版本仍不得宣称适合保存秘密。
- 内容继续以名称和字符串内容为核心领域模型；Markdown、TOTP 动态验证码、代码高亮和脚本预览/执行通过独立内容处理器扩展，不把这些用途固化为节点类型或业务字段。

## 6. 技术选择

### 6.1 桌面应用

- 桌面框架：Tauri 2
- 核心语言：Rust
- 界面：React + TypeScript
- 多语言：i18next + react-i18next；界面文字使用独立语言资源，首批提供简体中文和英文
- Windows 渲染：WebView2
- macOS 渲染：WKWebView
- Linux 渲染：WebKitGTK

Tauri 负责桌面窗口、安装包、系统能力和安全边界。界面由跨平台 Web 技术实现，但产品是本地桌面应用，不部署成供用户直接访问的网站。

### 6.2 后端与存储

- 领域模型和业务规则：纯 Rust crate
- 应用用例：纯 Rust crate
- 后端协议：与供应商无关的 HTTP/JSON API
- 接口描述：由 Rust 类型生成 OpenAPI，前端从契约生成 TypeScript 客户端
- 首个远程后端：Cloudflare Worker
- 首个远程数据库适配器：Cloudflare D1
- 测试适配器：内存实现

Cloudflare 代码只允许存在于 Worker 入口和 Cloudflare 存储适配器中。领域层、应用层和桌面前端不得依赖 D1、Wrangler 或 Cloudflare SDK。

桌面端保存和远端同步是两条独立路径：本地工作区负责输入期间的低延迟持久化和异常恢复；未加密模式下，供应商无关 API 的 `PUT /v1/nodes/{node_id}` 以完整节点为单位接收失焦提交。切换后端不得改变本地编辑和整节点提交边界。

密码保护模式的远端同步不能把明文节点交给现有节点 API。客户端必须先加密完整工作区或有界同步单元，远端后端只保存带版本的密文封装和完成同步所需的最少元数据。Cloudflare Worker 与 D1 只是这种不透明存储的一个可替换适配器；启用端到端加密后，后端不负责名称搜索、内容理解或解密冲突合并。当前桌面应用尚未连接 Cloudflare，现有 Worker/D1 契约也尚未扩展为密文同步契约。

### 6.3 内容展示与处理扩展

- `Node` 仍然只有稳定 ID、名称和字符串内容；Markdown、验证码、脚本等能力不进入领域类型，也不改变 `Reference`。
- 第一种富文本方式优先采用可移植的 Markdown 源文本：编辑时仍是普通文本，预览时经过禁用危险 HTML 的安全渲染。不得把编辑器私有 JSON 作为唯一内容格式。
- 内容处理器通过供应商无关的 `ContentProcessor` 边界读取一个已解锁节点的只读快照，返回受约束的展示数据和显式操作；React 视图不直接执行节点中的任意代码。
- 处理器选择属于展示/使用方式，可保存为与节点领域数据分离的视图元数据。默认保持纯文本；自动识别只能提供建议，不能在没有用户确认时执行脚本或暴露秘密。
- TOTP 处理器可以读取明确选择的节点或 `otpauth://` 内容，在本机内存中实时计算验证码和剩余时间。验证码是派生显示结果，不写回节点、工作区、日志、向量缓存、远端同步或模型输入。
- 脚本处理分为只读预览与显式执行。预览只做语法高亮；执行必须经过 Rust 权限代理，默认禁止文件、网络、环境变量和其他节点秘密访问，并具备运行时限、输出上限、取消和独立进程回收。输出只有在用户明确操作后才保存为节点内容或新节点。
- 第三方处理器不能作为任意 JavaScript 注入 WebView。将来开放扩展时必须使用隔离进程、明确能力声明和逐项授权；首批只实现经过审查的内置处理器。

### 6.4 LLM 辅助文档导入

杂乱外部文档的导入是独立的增量用例，不复用完整工作区导入/恢复，也不改变 `Node + Reference` 领域模型。第一版只读取 TXT、Markdown 和用户主动粘贴的纯文本；PDF、Word、网页、图片与 OCR 属于后续的内容提取适配器。

本地流程为“本地读取原文 → 确定性分段 → 本地 LLM 逐段提取 → 规范化名称合并 → 匹配现有节点 → 生成导入草稿 → 人工编辑/排除 → 画布差异预览 → 单事务确认写入”。外部强模型流程为“独立离线页面脱敏 → 用户主动发送脱敏文本 → 外部模型返回保留占位符的有界 JSON 草稿 → 原页面在本地安全还原并下载 → 桌面端载入 → 同一草稿和预览边界”。模型和外部文件都没有直接写入工作区的权限：取消、失败、锁定、工作区被替换或分析结果过期时，正式节点和引用都保持不变。

- 普通程序负责文件读取、大小限制、分段、哈希、去重、名称唯一性、引用端点和最终事务；LLM 只返回有界的候选名称、内容和引用名称。
- 文档正文和已有节点摘要都是不可信数据，不作为模型指令执行。Rust 命令边界使用严格 JSON Schema、字符/数量/token 上限和结果复验；未知字段、过量结果、空名称、悬空引用及重复项不能越过草稿边界。
- 分段按段落与行边界确定性完成，超长段落才使用有界重叠；不得把整份长文档一次塞入小模型。逐段结果按规范化名称合并，相同内容不重复追加。
- 与现有非空名称精确匹配的候选只标记为“匹配已有节点”，第一版不自动修改其名称或内容；其余候选使用新 UUID。近似名称只作为以后可加入的向量匹配建议，不能静默合并。
- 每次导入建立一个名称唯一的来源节点。来源节点保存原文件名/粘贴来源、SHA-256、导入时间、分析方式和完整原文；所有选中的提取结果都引用来源节点。工作区加密时这些内容随工作区密文保存，任何正文都不进入日志或遥测；只有用户主动提交的脱敏版本可以进入外部 AI。
- 草稿允许排除候选、修改新增节点名称和内容、选择模型提出的引用；已有节点只读。确认前必须再次对当前工作区验证名称和端点，并基于当前版本重新生成目标快照。
- 画布预览复用工作区差异语义，显示新增节点、已有匹配、引用和实际布局。确认写入是一个可撤销的历史步骤和一次本地持久化提交；不能逐节点半完成。
- 应用不直接调用远端 LLM，不保存远端令牌，也不后台发送文档。外部分析通过供应商无关的文件契约进入：`schemaVersion=1`、`kind=linked-info-document-import-draft`、来源文本和最多 64 组受限候选。文件按不可信输入复验大小、字符数、名称、正文、候选数和引用端点。

当前切片已经完成：顶部入口支持 UTF-8 TXT/Markdown、粘贴文本和外部分析 JSON 草稿；Rust 文件命令受工作区授权代次保护，本地 Qwen/llama.cpp 命令使用严格 JSON Schema 和双端边界校验。前端已经实现确定性分段、跨段合并、外部草稿复验、精确已有名称匹配、可编辑/可排除草稿、来源节点、实际布局差异预览，以及确认时基于最新工作区重建的一次性历史事务。取消分析会停止本地模型并丢弃内存草稿；导入不会修改或删除已有节点，应用也不会直接调用远端模型。

文档导入必须有独立于真实工作区的可重复评测集。仓库内保存原创的合成混乱文本与人工标准答案，覆盖账号与服务关系、订阅、优惠码、脚本、来源、冲突更新、格式混排、跨段关系、提示注入和无可提取信息等场景；测试数据只能使用虚构账号、无效域名和无效秘密，不能从用户工作区、导出包、剪贴板或屏幕采集。

评测结果至少分别报告节点精确率/召回率、引用精确率/召回率、内容事实覆盖率、重复名称和禁止名称命中。它不参与正式工作区读写，也不能因为模型分数较低而自动修改提示词、模型或用户数据。模型或提示词变更前后必须使用同一版本夹具比较；人工确认仍是产品安全边界，评测分数不能替代导入预览。

本地小模型的导入提示与完整输入/输出示例使用仓库内同一份版本化契约，Rust 正式路径和离线评测器不得各自维护副本。固定夹具的首轮实测证明纯抽象规则会让 1.7B 模型输出泛称摘要节点；加入两个完整示例后节点 F1 明显改善，但引用、事实归属、标题过滤与提示注入仍不足。因此导入继续保持人工草稿状态，不宣传为自动准确导入；后续应优先拆分提取阶段或增加可选强模型，不继续无限追加提示规则。

Qwen3 1.7B Q8_0 在 10 条 `linked-info-synthetic-zh-v1` 固定夹具上的当前开发基线为：节点精确率 0.5152、召回率 0.7083、F1 0.5965；引用精确率 0.3000、召回率 0.2000、F1 0.2400；事实覆盖率 0.2000；禁止名称命中 5 次。该结果只说明当前提示契约在这组原创用例上的行为，不代表公开数据集或真实个人资料的准确率，也不构成发布质量门槛。

下一轮实验把单段导入拆为三个串行、受约束的本地推理阶段，但最终继续返回同一个 `nodes + referenceNames` 草稿契约：

1. **具体对象提取**：只提取原文明确命名且可以稳定复用的账号、服务、脚本、项目、工具、优惠码等对象；不创建日期、价格、状态、字段名、章节名、秘密值或泛称节点。
2. **关系记录生成**：输入原文和第一阶段候选，只生成“事实属于多个对象组合”时需要的普通记录节点；不决定引用，也不重复创建参与对象。只有一个参与对象且事实可以直接归属于它时不生成关系记录。
3. **候选间引用判断**：输入前两阶段的带编号候选，只能从编号枚举中选择有方向的直接引用；不能创建或改名，不能自引用。普通关系记录引用参与对象，脚本可引用明确依赖，优惠码可引用明确适用的服务或账号。

每一阶段使用独立 JSON Schema、独立数量上限和双端复验。记录阶段只能看到原文和已经验证的实体；引用阶段只看到已经验证的节点，不再重复发送原文。每次调用还必须在发送前通过 3,000 token 的保守输入预算，给 4,096 上下文中的结构化输出留出空间，绝不依赖运行时静默截断。任一阶段失败、超时、取消或返回未知编号时，整段失败且不产生部分草稿。前两阶段规范化后名称必须唯一，合并候选总数仍受原有 24 个节点上限约束；第三阶段只输出已知端点，最后再组装现有响应。关系记录声明的参与方引用由确定性后处理补全，不再依赖第三阶段重复猜测；当原文包含密码、令牌、2FA、恢复码等敏感标记且候选中恰有一个账号和一个服务时，后处理必须新建或补全关系记录并原样保存整段正文，秘密值不得进入名称，容量不足则整段失败而不是静默丢失。文档分段、跨段合并、人工编辑、画布差异预览和单事务确认保持不变。

多阶段提示也必须使用一份由正式 Rust 路径与离线评测器共享的版本化契约。同一 10 条夹具必须同时保留单阶段 `0.5965 / 0.2400` 节点/引用 F1 作为对照；只有在相同模型、量化、运行参数和标准答案下复测，才允许声称多阶段改善。三倍调用次数带来的延迟作为明确成本记录，不用降低验证边界换速度。

三阶段 v2 契约、确定性敏感关系后处理及 llama.cpp 固定种子 `42` 在同一 10 条夹具上的当前可复现基线为：Qwen3 1.7B Q8_0 的节点精确率 0.6800、召回率 0.7083、F1 0.6939；引用精确率 0.3125、召回率 0.3333、F1 0.3226；事实覆盖率 0.2857；禁止名称命中 1 次；整轮约 139 秒。Qwen3 4B Q8_0 的节点精确率 0.7200、召回率 0.7500、F1 0.7347；引用精确率、召回率和 F1 均为 0.3333；事实覆盖率 0.3429；禁止名称命中 0 次；整轮约 241 秒。4B 有实际但有限的质量提升，CPU 耗时约为 1.73 倍，因此 1.7B 保持默认，4B 只作为质量优先选项。两者都实际生成了包含测试密码与恢复码原文、并引用账号和服务的关系记录；严格评分因模型名称“登录记录”与期望名称“测试记录”不同而未把该节点及其引用计为命中。此前未固定种子的分数只能保留为历史观察，不再用于跨版本精确比较。

提示注入夹具在记录阶段生成了违反名称/参与方边界的结果，被验证器拒绝。正式应用对该分段返回失败且不生成部分草稿；离线评测将该用例记为零预测并保留失败阶段。这保证恶意候选不会跨过草稿边界，但也会连同同段合法信息一起拒绝，尚未做到安全地局部恢复。当前结果足以把三阶段设为本地 1.7B 的正式导入路径，但仍只能称为人工审核的候选生成器。

公开数据只作为补充层：CLUENER2020 用于中文实体召回，DocRED 或 Re-DocRED 用于跨句实体关系。由于这些数据不等价于“个人杂乱笔记到任意节点图”，不得把公开榜单分数宣传为产品导入准确率。仓库提供离线转换器，但不自动下载、不提交第三方原文或转换结果；使用者必须从官方来源取得数据并自行核对许可，生成物放在忽略的 `artifacts/` 中。

## 7. 架构边界

```text
桌面视图（画布 / 将来的其他视图）
        |
        | 统一的前端数据与命令接口
        v
React 桌面应用层
        |
        | 供应商无关的 API 客户端与契约
        v
Rust 应用用例与领域模型
        |
        | 存储接口
        v
存储适配器
  |-- 内存适配器（测试）
  |-- Cloudflare Worker + D1（首个实际后端）
  |-- 其他后端或数据库（可独立加入）

本地工作区存储
  |-- 输入防抖保存、创建与失焦立即刷新、远端失败待同步

智能引用服务
  |-- 本地 ONNX 文本向量适配器
  |-- 可配置的远端嵌入接口适配器
  |-- 派生向量缓存与候选排序（不进入领域数据和导出包）

内容处理服务
  |-- 纯文本与安全 Markdown 渲染
  |-- 本地 TOTP 等只读派生展示
  |-- 受权限代理约束的脚本预览与显式执行
```

有两条独立边界：

1. 更换画布为列表等视图，不改变节点、引用和后端数据契约。
2. 更换 Cloudflare 或数据库，只替换后端入口或存储适配器，不改变桌面交互和领域规则。

## 8. 数据可移植性与安全边界

应用必须提供独立于 Cloudflare 的完整导出格式，至少包含：

- 格式版本和生成时间。
- 全部正式节点，包括稳定内部 ID、可空名称和内容。
- 全部引用的源节点 ID 与目标节点 ID。
- 与领域数据分离的画布布局。

备份恢复测试属于发布验证的一部分。Cloudflare 快照和 D1 导出只能作为额外保护，不能代替应用自身的完整导出。

密码保护与端到端加密规则：

- 当前桌面端仍只在本地运行，没有连接 Cloudflare、D1 或其他远端同步后端。未启用加密的工作区是明文 Rust 文件，界面必须明确提示只适合非秘密信息；用户启用密码保护后才允许把它视为秘密存储。
- 加密是用户主动设置密码后启用的工作区模式；未设置密码时允许继续管理非秘密信息，但界面必须清楚标记为明文工作区。应用不得保存用户密码本身。
- 创建加密工作区时生成随机 256 位数据密钥；使用 Argon2id、每个工作区独立随机盐和版本化参数从密码派生密钥加密该数据密钥。工作区、恢复副本、自动快照和默认导出使用带认证加密的版本化封装，首个实现采用 XChaCha20-Poly1305，并为每次加密生成独立随机 nonce。
- 名称、内容、引用和布局都属于加密载荷；文件头只保留格式版本、KDF/AEAD 参数、盐、nonce 和解锁所需的密钥封装。密码错误、认证标签错误或版本不支持必须快速失败，不能部分加载或用空工作区覆盖密文。
- 改密码只重新封装现有随机数据密钥，不需要重写所有节点。独立的“轮换数据密钥”会先撤销当前明文会话，再生成新的随机数据密钥，重加密正式工作区、恢复副本和全部本机历史；若已启用系统快速解锁，还会替换当前设备凭据。应用管理目录之外的手动导出不会被改写。
- 数据密钥轮换使用可恢复事务：先在独立待提交目录写入并逐份解密验证全部新密文，清单从 `preparing` 切换到 `ready` 后才允许提交，vault 元数据最后写入并作为提交点。启动恢复会丢弃未完成的 `preparing` 事务，或幂等完成 `ready` 事务；成功或失败后应用都保持锁定，必须重新解锁。
- 应用锁定时必须停止能读取明文的本地模型与内容处理进程，并尽力清除内存中的密码、数据密钥、TOTP 当前值和脚本输出。
- 向量等由明文派生且可能泄露语义的设备缓存，在加密工作区中也必须使用工作区数据密钥保护，或不持久化。当前实现会在启用加密时删除 SQLite 向量缓存，之后只使用本次解锁会话的 64 MiB 内存 LRU；模型权重本身不是用户数据，不随工作区加密。
- 从现有明文文件迁移时，先写入并重新解密验证新的密文文件，再原子切换正式工作区；验证前不得删除原文件。应用管理范围之外的旧明文导出无法自动回收，必须明确提醒用户自行处置。
- 加密工作区的远端同步使用 TLS 传输客户端已经加密的密文封装。Cloudflare 或其他存储提供者不得获得解密密钥，也不能读取名称、内容、引用或布局；最少同步元数据仍可能泄露文件大小、更新时间和版本次数，界面与文档必须如实披露。
- “远端同步存储”和“远端 AI”是不同边界。存储后端可以只保存密文；远端嵌入或 LLM 必须看到明文才能理解内容，TLS 只保护传输途中而不是阻止服务商读取。密码保护模式默认禁止把解锁后的工作区发送到远端 AI；在没有本地筛选和明确排除秘密节点之前，不允许复用当前全库远端向量流程。
- 加密只解决保密性和篡改检测，不代替备份。加密工作区的恢复副本和未来轮换备份必须同样加密，且必须验证真实的解锁、导出、导入和恢复流程。

解锁方式采用以下已确认范围：

- 只支持“主密码”和“系统快速解锁”两类入口；当前不增加应用短 PIN、TOTP 解锁、Passkey、硬件安全密钥或独立恢复密钥。
- 主密码封装始终是工作区的可移植根入口，负责换设备、换系统和导入加密备份。系统快速解锁是可选的当前设备入口，不能替代主密码，也不进入工作区导出、备份或远端同步。
- “系统快速解锁”是统一产品名称，但其最低语义不是“系统会话已经登录即可读取凭据”，而是每次启用和每次解锁都必须重新通过平台用户验证。Windows 使用 Windows Hello 的 PIN、指纹或人脸确认后才允许读取 Credential Manager 中的设备密钥；取消、失败、未配置或平台服务不可用时必须保持锁定并回退到主密码。macOS Keychain 与 Linux Secret Service 只有在适配器能够保证本次操作发生明确的用户验证时才允许暴露为可用，不能把已解锁的系统钥匙串误称为快速解锁。
- 启用系统快速解锁时生成独立随机设备解锁密钥，由平台安全存储保管；工作区元数据只保存这把设备密钥对应的数据密钥封装和非秘密提供者标识。不得把主密码本身写入系统安全存储。
- 主密码和系统快速解锁采用 OR 语义，任一入口都可以解开同一把工作区数据密钥。修改主密码不应破坏已登记的系统快速解锁；关闭系统快速解锁只删除设备封装和平台凭据，不重加密节点。
- 系统快速解锁不是第二因素，也不提供跨设备恢复。在 Windows 上，Credential Manager 的通用凭据可以被当前登录用户上下文中的进程读取，因此该功能主要抵御工作区文件被单独复制后的离线读取，并提供免输主密码的便利；不能宣称它能够抵御已经控制当前系统会话的恶意程序。
- 系统快速解锁的用户验证、登记、读取、删除和故障回退必须在 Rust 边界完成。React 只能发出启用、解锁或禁用意图并提供本地化的系统提示文案，不能读取设备解锁密钥。首个经过验证的适配器只面向 Windows；macOS 与 Linux 在各自的逐次用户验证适配器完成前显示为不可用，不能退化成单击即解锁。

基本生命周期规则：

- 删除节点属于领域和存储契约的一部分，不只是在画布中隐藏。删除前必须明确确认；删除后同时移除该节点作为源或目标的全部引用，以及对应画布布局。
- 远端存储删除节点和清理引用必须在同一个原子操作中完成。删除不存在的节点返回明确的“节点不存在”，不能静默伪装成功。
- 导出包使用应用自有、带版本号的 JSON 格式，包含生成时间、全部节点、全部引用和全部布局；不包含 Cloudflare 凭据、应用密钥或仅属于当前设备的筛选状态。
- 导入必须先完整解析并校验格式版本、节点 ID、非空名称唯一性、引用端点和布局完整性。任何一项无效都不得部分写入或覆盖当前工作区。
- 导入有效文件后仍需用户确认覆盖。正式替换前先在本地保存一份当前工作区恢复副本；确认后以完整工作区为单位写入本地存储。
- 桌面应用通过本地工作区持久化接口读写快照，不在 React 视图中直接调用 `localStorage`。本地快照与未来远端同步是两条独立边界：本地保存始终存在，远端提供者失败不得阻断本地编辑。
- 正式 Tauri 桌面端由 Rust 在系统应用数据目录保存 `workspace.v1.json` 和 `workspace.recovery.v1.json`。写入必须先创建同目录临时文件、刷新文件内容，再以操作系统原子替换能力提交；WebView 不能获得任意应用数据路径的文件权限。浏览器开发模式继续使用原有 `localStorage` 适配器。
- 首次升级到 Rust 文件存储时，仅在对应 Rust 文件不存在且旧浏览器快照通过完整校验时执行一次迁移；新文件落盘成功后删除对应旧副本。Rust 文件已经存在或任一旧副本损坏时禁止用旧数据覆盖或静默删除原始文本。
- Tauri 关闭窗口时先阻止默认关闭，等待当前完整工作区写入 Rust 文件成功后，再由 Rust `AppHandle::exit(0)` 走应用退出与清理流程；不能在 `onCloseRequested` 回调内等待 WebView 窗口 `destroy()`，否则可留下无法正常退出的进程。写入失败则保持窗口打开并显示保存错误。浏览器模式仍使用同步本地适配器处理 `beforeunload`。
- 桌面端必须保持单实例。第二次启动不创建新的工作区写入者，而是显示、取消最小化并聚焦已经运行的主窗口，避免两个进程产生最后写入覆盖。
- 运行中的正式工作区、持久化快照和导出包使用同一组不变量：UUID 规范化后唯一、非空名称规范化后唯一、每个节点恰好一个有效布局、所有引用端点存在且不重复。编辑产生的无效名称只能作为未提交草稿存在，不能进入自动保存或导出快照。
- 本地持久化加载必须区分“从未保存”和“已有数据但无法解析或版本不受支持”。后一种情况必须停止自动保存、保留原始文本并向用户提供导出原始数据或明确清空的入口，禁止静默裁剪后覆盖原数据；恢复副本执行同样的严格校验。
- `WorkspacePersistence` 由桌面组合根注入应用组件，并使用异步加载/保存契约，使视图无需修改即可替换为 IndexedDB、文件或其他本地实现。
- 外部导入的 UUID 在唯一性、布局和引用校验前统一转换为规范小写形式，避免大小写不同但语义相同的 ID 在 Rust/D1 边界发生碰撞。

安全约束：

- Cloudflare API Token 只用于开发和部署，不得进入桌面安装包。
- 桌面应用访问远程后端时使用应用级身份认证，不使用 Cloudflare 账户级令牌。
- 前端不得直接执行 SQL，也不得直接访问 D1。
- 所有外部输入在 API 边界完成类型和约束校验，内部用例只接收有效数据。
- 内容处理器只能在工作区解锁后通过受控应用接口取得所需明文；TOTP 秘钥、脚本输出和解密后的内容不得进入遥测、崩溃报告或普通日志。

## 9. 代码组织

```text
apps/
  desktop/                 Tauri 2 + React 桌面应用及画布视图
  cloudflare-worker/       Cloudflare Worker 入口
crates/
  domain/                  Node 与 Reference 领域模型及约束
  application/             节点、引用、搜索和筛选用例
  contracts/               API 请求、响应和 OpenAPI 契约
  storage-port/            正式数据存储接口
  storage-memory/          测试用内存适配器
  storage-d1/              Cloudflare D1 适配器
```

本地编辑恢复和画布视图状态属于桌面端能力；如果后续确实需要跨设备同步，再单独扩展同步状态契约，首版节点 API 仍保持完整节点提交。

## 10. 实施阶段

### 阶段一：修正领域骨架

- 删除现有 `InformationType`、`FieldDefinition`、`InformationContext` 和 `RelationType` 方向的领域设计。
- 将核心模型收敛为 `Node` 和 `Reference`，并将画布布局排除在领域节点之外。
- 实现可空名称、非空名称唯一、稳定 ID 引用和引用完整性规则。
- 同步修改应用用例、存储接口、内存适配器和测试。

完成标准：领域代码只表达已确认的节点与引用模型，测试覆盖名称约束、改名后引用稳定以及按目标节点查询直接引用者。

### 阶段二：后端契约与 Cloudflare 适配器

- 定义供应商无关的节点、引用、搜索和筛选 API。
- 实现 D1 数据结构、迁移和存储适配器。
- 在 API 边界实现名称与引用约束校验。
- 保证 Cloudflare 类型不进入领域层或桌面前端。

完成标准：同一套应用测试可针对内存适配器和 Cloudflare 测试环境运行，行为一致。

### 阶段三：桌面最小工作流

- 实现本地工作区自动保存、恢复和节点整体提交边界。
- 实现节点新建、编辑、删除和引用选择。
- 实现默认名称搜索、节点内引用展示，以及按一个或多个目标节点以 AND 语义筛选共同引用者。
- 实现画布内直接创建、节点内编辑和空名称筛选。

完成标准：用户可以在画布上立即创建并编辑节点、建立引用，查看每个节点已引用的目标，并通过名称、空名称或多个被引用节点的 AND 组合重新找到信息；异常关闭不丢失本地输入，远端不按字符提交。

### 阶段四：画布视图

- 实现可平移、缩放、拖动节点和创建引用的画布。
- 画布节点显示名称和内容摘要。
- 独立持久化坐标、宽度和折叠状态。
- 验证更改布局不会修改节点或引用数据。

完成标准：核心录入和查询工作可以在画布完成，布局数据与领域数据边界清晰。

### 阶段五：备份与发布

- 实现带版本格式的完整导出、严格校验导入、覆盖确认和导入前本地恢复副本。
- 实现设备本地自动备份历史：首次成功保存变更后立即建立一份，后续仅在内容变化且距最近快照至少一小时时建立；默认最多保留 30 份，并同时受 512 MiB 总上限和 90 天硬保留期限约束。数量或容量回收至少保留最新一份，超过保留期限的历史不例外保留；若当前工作区长期未变化，旧历史到期后下一次检查会建立一份内容相同但时间有效的新快照。
- 自动备份保存于独立的 `workspace.backups.v1` 目录，不进入手动导出、远端同步或 Git。它记录的是成功原子提交后的完整主工作区文件；明文工作区的历史为明文，启用加密时必须把既有历史与主工作区一起原子迁移为密文，之后的历史直接复制认证加密后的主文件，不额外持有明文副本。
- 设置页列出备份时间、文件大小和可读状态。读取历史时必须在 Rust 解密边界验证认证标签，再由前端共用工作区严格验证器校验节点、引用和布局；损坏、版本不支持或无法解密的快照只能报告错误，不能进入替换确认。替换确认使用独立的只读画布预览，不使用节点数量弹窗：默认在同一坐标系叠加当前与恢复后布局，并可切换“恢复前 / 叠加对比 / 恢复后”；新增、删除、内容变化和位置变化分别用颜色、原位置虚影与移动指示线表达，引用增删也直接显示在线上。预览允许平移缩放但不能编辑，确认前不得修改当前工作区；完全一致时禁用恢复，取消后返回进入预览前的页面。
- 从历史恢复沿用导入替换事务：先把当前工作区写入单独的恢复副本，再保存所选历史并刷新运行时状态。自动历史是同设备上的误操作恢复能力，不替代用户保存到其他设备的加密导出。
- 生成 Windows 安装包。
- 验证升级后的数据迁移和旧备份兼容性。

完成标准：新安装环境可以从导出包恢复全部节点、引用和布局；设备本地历史按时间与容量有界轮换，明文历史不会残留在已经加密的工作区中，损坏历史不会覆盖当前数据；Windows 安装包可完成核心工作流。

### 阶段六：智能引用第一层

- 定义供应商无关的向量提供者、模型配置指纹和候选结果类型。
- 实现普通 CPU 可运行的本地 ONNX 文本向量适配器，以及可配置地址和模型名的远端嵌入接口适配器。
- 实现本地模型目录、固定版本下载、详细进度、安全取消、续传、校验和缓存状态展示；设置页必须说明各模型的语言、下载量、资源负担、用途、局限和许可证。
- 实现名称与内容组装、长文本有界分段、余弦相似度排序和无文本节点排除；相似节点只作为证据和独立的“相关信息”结果。
- 实现基于现有引用图的候选传播：从有限数量的相似记录收集其出向引用，按相似证据权重聚合推荐分，展示支持数量，并确保相似记录本身不会被直接当作自动引用候选。
- 将会话内无限 `Map` 替换为默认 64 MiB 的内存 LRU，并实现独立 SQLite 持久化向量缓存：`Float32 BLOB`、512 MiB 默认磁盘上限、基于模型指纹/文本角色/内容哈希的键、按节点文本局部失效、按模型切换命名空间、LRU 回收和单独清空。缓存文件不进入工作区、备份、导出或同步。
- 在设置页实现提供方式选择、远端连接参数、会话级令牌、阈值自动引用开关和阈值配置。
- 在节点交互中实现明确触发分析、推荐引用与相关信息分区展示、推荐分和支持数量说明、手动确认，以及阈值以上普通引用的增量创建。

完成标准：同一个节点分析流程可以无须修改画布或领域模型而切换本地和远端向量适配器；账号等同类记录只作为相似证据，其共同引用的节点才进入推荐引用列表；不开启自动引用时不修改正式引用，开启后只在用户明确触发的一次分析中添加达到推荐分阈值的普通引用；模型文件、令牌、向量和分数均不进入工作区或导出包；重启后可复用相同配置和文本的持久化向量，内存与磁盘缓存达到上限时能回收旧条目，并且可在不影响正式数据和模型文件的情况下单独清空向量缓存。

### 阶段七：本地 LLM 复核

- 定义供应商无关的 LLM 请求、候选、响应和运行时接口；候选使用临时编号，响应在应用边界严格验证。
- 实现候选池构建：合并图传播候选与已被引用节点，排除源节点和已有引用，并为每个候选选择有限的相关历史示例。
- 固定并打包跨平台 `llama.cpp` CPU sidecar；Rust 管理本地回环监听、随机会话密钥、健康检查、单任务互斥、有限线程、超时和退出清理。
- 实现 Qwen3 1.7B Q8_0 固定版本模型的检查、续传下载、进度、取消、SHA-256 校验和按需加载。
- 在设置页增加本地 LLM 复核开关、模型详情和下载状态；在结果页分开显示 LLM 建议、不确定候选、基础候选和相关信息。
- 启用 LLM 时暂停第一层阈值自动引用；响应失败或无匹配时不修改正式引用，也不创建新节点。

完成标准：在不改变 `Node + Reference` 领域模型的前提下，同一次“智能引用”可以选择性经过本地 LLM；LLM 只能返回本次候选编号，所有建议均需用户确认；模型和 sidecar 全程本地运行，普通 CPU 推理不会占满全部逻辑核心，下载可取消续传，应用退出不遗留子进程；关闭 LLM 后第一层行为与此前一致。

### 阶段八：密码保护与加密工作区

- 在 Rust 持久化边界实现版本化加密封装、Argon2id 密码派生、随机数据密钥和 XChaCha20-Poly1305 认证加密；React 只提交解锁/锁定意图，不接触持久化密钥材料。
- 实现创建密码、解锁、锁定和改密码生命周期；锁定时停止本地 LLM 与内容处理进程，并使未完成的明文操作快速失败。
- 把正式工作区、恢复副本、默认导出和含语义的向量缓存纳入加密边界；实现明文工作区到密文工作区的原子迁移和失败恢复。
- 为未来远端同步定义供应商无关的密文封装契约。第一步只完成本地加密和契约测试，不因此自动连接或部署 Cloudflare。
- 明文模式继续可用，但必须明确提示只能用于非秘密信息；密码保护模式默认禁用远端 AI，不允许当前全库远端向量流程读取解锁后的工作区。

完成标准：正确密码可以恢复全部节点、引用和布局；错误密码、损坏密文和不支持版本都不会覆盖现有文件；改密码无需重写领域数据；锁定后没有继续持有明文的模型或处理进程；加密导出可以在新环境完整恢复；远端密文契约不向存储适配器暴露节点名称、内容、引用或布局。

本地加密第一切片已经完成：Rust 已实现版本化密钥封装、Argon2id、XChaCha20-Poly1305、原子明文迁移及中断恢复；桌面端已接入启动解锁、启用加密、立即锁定、修改密码、加密导出和加密导入。错误密码和认证失败会快速失败，锁定会卸载前端工作区、停止本地 LLM、取消嵌入模型下载并释放空闲嵌入模型。第二切片实现按上述规则登记、使用和关闭系统快速解锁，并保持旧加密元数据兼容；不增加恢复密钥、TOTP 或 Passkey。加密备份只能导入已启用加密的工作区，且明确排除设备解锁封装，避免把当前设备入口带入导出。远端密文同步契约仍是后续独立工作，不影响本地加密切片。

### 阶段九：解锁后安全、供应链与异机灾备

按依赖顺序实施，前一层未满足时不以后面的能力掩盖风险：

1. **严格锁定语义**：Rust 持有唯一授权状态；Windows 会话锁定、休眠和用户切换立即锁定；提供默认 15 分钟及 5/30 分钟可选闲置超时。锁定先撤销明文权限和推进任务代次，再清理模型、令牌和界面；保存或清理失败不能保持授权。嵌入、LLM 和内容处理结果携带任务代次，锁定前任务的迟到结果必须丢弃。
2. **敏感操作重新认证**：修改主密码、启用/关闭系统快速解锁、完整导出、清除历史与恢复副本、销毁工作区、设备授权和密钥轮换要求当前主密码或新的系统用户验证。授权在 Rust 产生、限定用途并短时有效，不能由 React 复用长期解锁状态替代。
3. **CSP 与权限收紧**：正式 WebView 只加载打包资源和必要 IPC，禁止远端脚本、嵌入页面及任意联网；开发热更新使用独立策略。文件导入导出移入一次性 Rust 命令，移除主 WebView 的通用文本文件权限；显式声明 Tauri 自定义命令和窗口 capability，并在 Rust 命令边界继续校验锁定状态。
4. **依赖与发布供应链**：CI 增加 RustSec、生产前端依赖、许可证和来源检查，生成 SBOM 与产物 SHA-256；模型和构建时下载全部固定提交与哈希，GitHub Actions 固定完整提交 SHA。自用开发构建可保持未签名；构建来源证明、具备一致身份的 Authenticode 签名、可信时间戳和签名更新验证属于面向普通 Windows 用户公开发布前的门槛，不阻塞当前自用开发顺序。
5. **密码与数据密钥生命周期**：新密码提高到至少 15 个 Unicode 字符并使用本地阻止列表；Rust 侧对连续失败递增等待，Argon2id 参数保留升级路径。普通改密码继续只重新封装数据密钥，另提供原子重加密主文件、恢复副本和历史的数据密钥轮换。
6. **删除、剪贴板与日志**：区分当前节点删除、本机历史/恢复副本清除和整个工作区销毁；不宣称 SSD 单文件安全擦除。专用秘密复制在内容未改变时延时清空，普通复制不受影响；秘密、模型输入和脚本输入输出不得进入普通日志、遥测或上传崩溃转储。
7. **异机加密备份**：先实现追加式快照与显式恢复，不同时引入多设备冲突合并。定义供应商无关 `BackupTarget`；首个提供者采用 Worker + R2 保存客户端密文，D1 仅保存必要账户、设备和版本索引。每个目标独立报告最近上传、验证和全新配置恢复演练状态，再扩展 S3、WebDAV、本地目录或其他目标。

完成标准：锁定之后所有新明文命令失败且旧任务结果无法写回；敏感操作不能只凭已解锁界面执行；正式 WebView 没有通用联网或文件读写权限；CI 能阻止已知高危依赖和未固定下载进入发布；整个工作区可以通过主密码从至少一个异机加密快照在全新配置中恢复，远端提供者全程只见密文和已披露的最少元数据。详细威胁边界与发布门槛见 [SECURITY.md](SECURITY.md)。

阶段九必须严格按 1 → 7 执行，不能因为后续某个切片已经提前完成就跳过当前步骤。当前状态如下：

- 1～3 已完成：Rust 授权代次、闲置锁定、Windows WTS/休眠通知和模型迟到结果丢弃；限定用途、60 秒、单次消费的敏感操作授权；正式/开发分离的 CSP、最小事件 capability 以及 Rust 一次性文件导入导出。
- 4 已完成并通过 CI：RustSec、生产前端依赖审计、模型文件提交/大小/SHA-256、第三方 Action 完整提交 SHA、便携包 SHA-256、精确许可证门禁和 Rust/前端 CycloneDX SBOM 已接入。构建来源证明按上面的公开发布门槛后置，不阻塞后续自用开发。
- 5 已完成并通过 Windows CI：密码下限已提高到 15 个 Unicode 字符，连续失败采用有界递增等待；设置页提供独立数据密钥轮换，使用两阶段待提交目录重加密正式工作区、恢复副本、本机历史和当前设备快速解锁凭据，并覆盖准备中断、提交中断、无效历史及设备凭据切换测试。
- 6 已完成并通过 Windows CI：自动历史与恢复副本可独立清除；整个工作区销毁会严格锁定后删除设备快速解锁凭据、正式与待提交文件、恢复副本、自动历史和 vault，失败时不恢复明文授权。自动历史除 30 份与 512 MiB 外增加 90 天硬保留上限，并在每次解锁后的历史检查或新快照写入时回收过期项。加密工作区的节点右键菜单增加显式“复制秘密”：Windows 首个适配器复制完整节点内容，45 秒后仅在剪贴板序列未变化时清除，并在锁定、密钥轮换和正常退出时提前清除；普通复制不受影响。界面提示系统剪贴板历史、跨设备同步与进程崩溃仍可能保留内容。直接处理明文、密码、令牌、模型输入和脚本数据的模块禁止普通日志调用，并由 CI 源码规则守住该边界。
- 7 已完成并通过实际目标验收：供应商无关的密文快照契约、对象安全的 `BackupTarget` 端口、独立 Cloudflare 备份 Worker + R2、桌面 HTTPS 适配器、系统安全存储中的应用级令牌、本地多目标配置，以及设置页中的手动上传、完整下载校验、恢复预览和不替换当前数据的恢复演练均已落地。没有本地 vault 时也可用只驻留内存的端点和令牌下载快照，Rust 仅凭快照主密码建立五分钟的待确认恢复会话，画布确认后原子写入新的本地 vault，并把原本地明文工作区转为加密恢复副本。开发账户已部署正式与隔离演练 Worker；真实工作区密文已完成上传、完整下载 SHA-256 对照，并在最新 Windows 构建中以快照主密码完成独立临时 vault 安装、重新解锁、节点/引用/布局核对和临时数据清理，实际目标已成功记录 `lastRestoreTestAtMs`。隔离 CI 也通过“合成工作区加密 → R2 上传 → 完整下载 → 全新临时 vault 安装 → 主密码重新解锁 → 节点、引用和布局核对 → 删除测试对象”的端到端演练。下一步才扩展第二个备份服务商。
- 多服务商扩展的第二个实现采用一个通用 `S3 Compatible` 适配器，不为 Backblaze B2、Tigris、Oracle OCI 分别复制上传、列举、下载、校验和删除逻辑。界面提供这些服务商的配置模板以及“自定义 S3”，模板只负责默认端点/区域提示，最终仍落到同一适配器。现有 Cloudflare Worker + R2 目标保持独立，因为桌面端连接的是带应用级授权的自定义 Worker API，并非直接持有 R2 S3 凭据。
- 通用 S3 适配器已通过 Backblaze B2 真实目标验收：用户在最新 Windows 构建中完成受限 Bucket 凭据验证、密文快照上传、远端完整下载 SHA-256 校验和使用快照主密码的独立恢复演练，当前工作区未被替换。该结果证明已配置工作区的 S3 上传、列举、下载和恢复演练闭环；新设备无本地 vault 的临时凭据恢复仍由共用实现与自动化测试覆盖，不把本次实机操作误记为已经再次手工验收。Tigris、Oracle OCI 和自定义 S3 只是配置模板，未经过本次真实目标验收。

第 7 步采用以下边界：

- 远端快照复用现有带版本的加密导出封装，包含由主密码封装的数据密钥，因此不能上传只能由当前设备内存密钥解释的裸工作文件；系统快速解锁凭据、备份服务凭据和筛选状态仍不进入快照。
- `BackupTarget` 只接收和返回不透明密文字节、快照 UUID、创建时间、字节数与 SHA-256。上传、列举、下载、删除和客户端完整下载校验均通过该端口；提供者专属限制作为适配器能力报告，不写进领域契约。
- Cloudflare 备份使用独立 Worker 和独立应用级授权，不与当前未鉴权的 D1 节点 API 共用部署与权限。桌面端不持有 Cloudflare 账户级 API Token。
- 首版 R2 利用强一致对象读写和对象元数据完成追加快照、列举与下载，不为了尚未出现的账户/设备查询需求提前引入 D1。需要多账户、设备授权或复杂版本索引时，才在 Worker 内部增加最少 D1 元数据。
- Worker 对上传体采用流式转发，不把完整密文加载进 Worker 内存；Cloudflare 入口的请求体上限属于该适配器能力。下载后由桌面端重新计算 SHA-256，不能只相信服务端回传的自报哈希。
- 桌面适配器禁止 HTTP 降级、重定向、URL 内嵌凭据、查询参数令牌和无限响应体；应用级令牌只进入系统安全存储，非秘密配置文件只保存目标 UUID、名称、HTTPS 端点、凭据引用和最近成功时间。
- “完整校验”只证明远端字节与 SHA-256 一致；“恢复演练”还必须要求快照创建时的主密码，在独立临时目录建立全新 vault、再次用主密码解锁并逐字节核对完整工作区。只有全部成功且临时目录清理完成后，才能同时更新该目标的最近完整校验与最近恢复演练时间；失败不得写入成功时间，也不得替换当前工作区。
- 远端快照与创建它时的主密码绑定。普通修改主密码不会神奇地改写已经上传的历史快照；恢复旧快照可能需要创建该快照时使用的旧主密码。将来若提供远端快照重封装，必须逐个下载、验证、重新加密并原子替换，不能只修改服务端元数据。
- S3 兼容目标的本机配置只保存标准 HTTPS endpoint、region、bucket、受限 object prefix 和系统凭据引用；Access Key ID、Secret Access Key 与可选 Session Token 作为一个版本化凭据记录进入系统安全存储。整个目标配置使用系统安全存储中的独立随机设备密钥进行 HMAC-SHA-256 认证，读取凭据或执行网络与保留操作前必须先验证；旧版未认证配置只能在认证密钥尚不存在时迁移一次，防止回滚到旧格式绕过校验。配置文件不得出现任何一项凭据，界面也不得在保存后回显。
- S3 `ListObjectsV2` 不返回对象自定义元数据。为了避免每次列举额外对所有对象执行 `HEAD`，适配器使用版本化对象键编码创建时间、快照 UUID 和 SHA-256，并以服务端返回的对象大小组成 `BackupSnapshotMetadata`；不符合本应用键格式的其他桶对象一律忽略。下载后仍须重新计算 SHA-256，不能把对象键中的值当作完整性证明。
- S3 目标默认使用 path-style 寻址，以便 endpoint 与 bucket 分离并覆盖 B2、Tigris、OCI 和自建兼容服务；只允许 HTTPS、禁止 URL 用户信息/查询/片段、限制 bucket/region/prefix 长度与字符范围，并禁止自动重定向。相同 endpoint、bucket 与 prefix 的目标视为冲突；同一 endpoint 下的不同桶或前缀可以并存。
- 通用 S3 目标必须具有与 Cloudflare 目标相同的功能闭环：已配置工作区中的上传、列举、完整下载校验、恢复预览与恢复演练，以及没有本地 vault 时使用临时凭据完成的新设备恢复。只实现上传而没有新设备恢复不算完成。

第 7 步完成后的下一项是自动异机备份，采用以下最小生命周期，不把每次文字保存直接变成一次网络上传：

- 自动异机备份按目标独立启用，新增目标默认关闭；启用属于会产生持续网络访问和服务商用量的明确选择，不能因为目标验证成功而自动打开。
- 第一版只在桌面应用已解锁且运行时工作。一次有效工作区变更先完成本地持久化，再把所有已启用目标标记为待上传；画布纯平移、缩放、搜索和筛选不触发异机备份。应用关闭、Windows 会话锁定、休眠或工作区锁定时不启动新上传。
- 每个目标默认最短上传间隔为 24 小时。达到期限且存在待上传变更时才创建一个完整密文快照；同一目标同一时间最多一个上传任务，多次变更合并为下一份快照。手动“立即上传”不受间隔限制，并在成功后清除该目标当时已经覆盖的待上传状态。
- 待上传状态、启用状态、最短间隔、最近尝试时间和最近错误保存在本机非秘密目标配置中；不把节点正文、明文摘要或可用于猜测内容的裸哈希写入配置。离线、限流、服务商故障或凭据失效不得阻止本地保存，也不得回滚工作区；应用保持待上传并在下一次解锁会话或下一次有效变更后重试，不做无限紧循环。
- 多目标互相隔离：一个目标失败不阻止其他目标，状态分别显示。自动上传成功只证明密文已写入并重新列出，不能冒充完整下载校验或恢复演练；后两者仍保留各自时间和人工验证语义。
- 自动上传以任务开始时冻结的工作区快照为输入。上传期间发生的新变更继续保持待上传，不能因为旧快照成功而误清除；锁定代次变化或目标被移除后，迟到结果不得更新成功状态。

远端快照生命周期继续遵守以下语义，避免把“断开连接”和“删除备份”混成同一个危险操作：

- “从本机移除目标”只删除本机非秘密配置和系统安全存储中的该目标凭据，默认保留服务商上的全部密文快照。操作前重新认证，完成后应用不再能够列举、上传或恢复该目标；若以后重新填写相同连接信息，远端快照仍可读到。
- 单个快照可以在重新认证和明确确认后删除。删除只调用所选 `BackupTarget`，不影响其他目标、本地工作区、本机历史或恢复副本；提供者报告不存在时不得伪装成成功。
- “删除全部远端快照并移除目标”是独立的高风险操作，要求重新认证并输入目标名称。应用必须先完整列举该目标的快照，再逐个删除；任一远端删除失败时保留本机目标和凭据以便重试，并报告可能已经发生的部分删除，不能在失败后造成目标失联。
- 远端自动保留按目标单独设置，默认关闭。启用或修改会导致删除的保留规则必须重新认证；首版允许设置最大快照数和最大保留天数，快照超过任一限制即可进入清理候选，但始终保留最新快照以及最近一次通过恢复演练的快照。
- 自动保留只在该目标成功写入新快照后运行，不为定时清理增加后台常驻任务。清理失败不撤销已成功上传的快照，也不阻塞本地保存；目标记录最近清理时间和独立错误，下一次成功上传后重试。
- 删除远端对象不等于介质级安全擦除。服务商的对象版本、软删除、生命周期规则或审计日志仍可能保留密文与元数据；界面只能陈述应用发出的删除及提供者返回结果。
- 设置页按目标显示最近自动尝试、下一次满足上传间隔的时间、是否仍有待上传变更、最近保留清理和错误。多目标状态、删除和保留规则完全隔离，一个目标失败不得改变另一个目标的配置或快照。

当前只实施第 7 步；代码签名仍是对外发布门槛，不要求当前自用构建购买证书。

## 11. 当前工程状态与开发约束

现有工程骨架已经具备：

- Git 仓库与统一 Rust workspace。
- Tauri 2 + React + TypeScript 桌面应用骨架。
- 基于独立语言资源的简体中文、英文界面及语言选择持久化。
- 分层的领域层、应用层和异步存储接口。
- 内存存储实现、供应商无关的 HTTP/JSON 与 OpenAPI 契约，以及 Rust Cloudflare Worker。
- Cloudflare D1 初始迁移和独立 `storage-d1` 适配器。
- GitHub Actions 在 Windows 环境完成核心 Rust、Worker Wasm、前端和 Tauri 检查。
- 手动触发的 Windows 便携版构建工作流。

领域代码已经替换为 `Node` 与 `Reference` 模型；名称已经改为可空，并由存储保存边界只对非空名称原子保证唯一。内存适配器和应用用例覆盖名称搜索、多个未命名节点、直接引用者筛选以及改名后引用稳定。旧的“信息类型、自定义字段、固定上下文、关系类型”模型已移除。

阶段二已经完成：Worker 已按版本化契约提供节点创建、读取、更新、分页名称搜索、引用创建、移除和分页双向浏览接口，并通过 D1 binding 调用存储适配器。API 使用稳定错误码并提供 OpenAPI 文档，Cloudflare 类型没有进入领域层或桌面前端。当前没有创建或部署远程 D1 数据库，也没有实施尚未确定的应用级身份认证，因此不能把当前 API 直接作为公开服务部署。

阶段三和阶段四的本地主体框架已经完成：桌面端具备独立保存节点、引用和画布布局的本地工作区边界，以及 React Flow 画布的平移、缩放、节点拖动、自定义右键菜单、端口拖线建立引用、选中并移除引用、引用目标展示、多条件 AND 筛选、迷你地图和缩放控件。旧的“名称必填、先侧边表单后创建”流程已经替换为“节点立即落点、名称可空、节点内编辑、本地优先保存、整节点失焦提交”，并已增加清晰线性网格和空名称筛选。节点删除已经接入领域、内存存储、D1 存储、HTTP 契约和桌面确认流程，并会连带清理全部相邻引用及画布布局。

画布节点已经采用单一、持久化的堆叠顺序：布局数组从后到前表示遮挡层级，最后交互的节点移动到最前；React Flow 自带的选中抬升已经关闭，不再让“编辑焦点”和“画布选中”互相争夺层级。自动化测试覆盖顺序移动，真实重叠节点验证覆盖交互置顶与刷新恢复。

阶段五的本地数据生命周期已经形成闭环：桌面端使用带版本号的 JSON 导出完整节点、引用和布局；导入先进行全量严格校验，再显示替换确认；执行替换前保留当前工作区作为本地恢复副本。工作区运行时、持久化和备份共用同一验证器，UUID 会先规范化，重复名称不会进入正式快照；无法读取或版本不兼容的本地数据会停止自动保存，并提供原始数据导出和明确清空入口。桌面视图通过组合根注入异步 `WorkspacePersistence`；正式 Tauri 模式由 Rust 管理应用数据文件，浏览器存储只保留为开发适配器和一次性旧数据迁移来源。写入按调用顺序排队并使用同目录临时文件原子替换，关闭窗口前会等待队列刷新，然后由 Rust 触发应用退出；第二次启动只聚焦已有主窗口。上述数据不变量、迁移、写入顺序和关闭失败行为已经加入自动化测试与 CI。桌面端仍未接入远端 API；远端同步和身份认证不属于本轮主体框架完成条件。

阶段五的设备本地自动备份历史已经完成：首次有效变更保存后建立快照，后续内容变化时最多每小时一份，按 30 份、512 MiB 和 90 天三重上限轮换；设置页显示时间、大小、保留期限和损坏状态，恢复确认会对比当前与目标版本的领域数据、布局和视口，完全一致时不允许执行无意义替换。加密迁移会通过独立待提交目录把既有明文历史与主工作区一起改写为认证密文，失败或中断不会把历史误报为已加密。它与“导入前恢复副本”和“用户手动导出的可移植备份”是三种不同生命周期：历史快照用于同设备按时间回退，恢复副本用于一次替换操作的即时反悔，手动导出负责设备损坏或迁移后的恢复。

2026-08-10 00:20 Windows 记录过一次 `linked-info-desktop.exe` 的 `AppHangB1`。同一台机器上的 `RoxyBrowser.exe` 在此前约 52 秒也发生相同事件，二者的 WER 挂起参数 `P5=67246080` 相同；对应时间段没有显卡驱动重置事件。当前证据只能把范围缩小到共享的 Chromium/WebView/Windows 桌面运行栈，不能认定为画布业务逻辑根因。若再次出现，必须记录发生时间和此前最后一个操作，再对照 WER 与系统事件；在取得应用独有的稳定复现前，不以禁用 GPU或改写画布事件作为猜测性修复。

阶段六已经完成：本地与远端向量提供者、图传播候选、相关信息分区、推荐分、阈值自动引用、64 MiB 内存 LRU、独立 SQLite 向量缓存、下载详情和缓存清理均已接入。远端冷缓存的全工作区明文传输范围已经在设置与分析进度中披露。

阶段七已经完成：本地 Qwen3 1.7B 候选复核、固定 llama.cpp sidecar、模型续传与 SHA-256 校验、有界请求、结构化结果验证、手动确认和进程生命周期已经接入。Windows 下真实验证了强制结束主程序后 Job Object 自动回收 llama-server。小型重排模型、后台批处理和自动结果审计仍是使用证据驱动的候选功能，不属于已确认实现范围。

开发环境：

- Rust 工具链根目录：`D:\soft\toolchains\rust`
- Cargo 目录：`D:\soft\toolchains\rust\cargo`
- Rustup 目录：`D:\soft\toolchains\rust\rustup`
- Windows C++ 工具链：`D:\soft\toolchains\cpp\vs-buildtools-2022`
- Windows WebView2：已安装

编译与验证策略：

- 默认不在本机执行 Cargo、Worker 或 Tauri 编译。
- 日常检查和编译交给 Windows GitHub Actions，与当前实际使用环境保持一致。
- Windows 便携版使用手动触发的 GitHub Actions 工作流生成；macOS 和 Linux 仅在正式支持对应平台时恢复验证与打包。
- 只有必须依赖本机窗口的交互调试或用户明确要求时，才进行本地编译。

独立审查后的加固规则：

- 异步智能引用结果只能写回仍存在于当前工作区的源节点和目标节点；删除节点或替换工作区后，旧分析不得建立悬空引用，结果界面也不得继续提供失效目标。
- 远端向量分析在冷缓存时可能发送当前工作区全部非空节点的有界分段；设置页、分析进度和用户文档必须准确披露节点范围，不能把全库向量化描述成仅发送最终候选。
- 本地 LLM 请求除逐字段上限外必须有统一总预算；达到预算时先去掉示例和次要详情，再减少低排名候选，Rust 边界再次拒绝超预算请求。
- 正式 GGUF 文件只有通过固定 SHA-256 校验后才是“就绪”；校验标记绑定固定哈希、文件大小与修改时间，同长度损坏不得绕过校验。
- Windows llama.cpp sidecar 必须加入关闭即终止的 Job Object；正常退出仍执行显式停止，异常终止由操作系统回收子进程。
- 开发期 Windows 构建产物默认只保留 3 天，便携版必须把 EXE 与 `llama-runtime` 目录作为整体使用。

## 12. 待讨论事项

以下事项尚未获得明确决定，不能根据旧草稿或助手建议擅自补全：

1. **引用边界规则**：是否允许节点引用自己。相同方向的重复引用已经禁止；删除节点已经确定为明确确认后连带移除全部相邻引用。
2. **画布集合语义**：`canvas_id` 是只为未来视图隔离预留，还是首版就支持多个独立画布；同一个节点是否能出现在多个画布。
3. **内容搜索**：当前桌面搜索框只搜索名称；是否增加显式的“名称 / 内容 / 两者”全文搜索范围尚未确认。
4. **内容处理器选择方式**：已经确定由用户显式选择；纯文本是默认处理器。自动识别将来只能给出建议，不能自行切换处理器，更不能自动执行脚本。

上述事项不影响当前主体框架；后续功能涉及对应行为时必须先形成产品决定。

## 13. 内容扩展前的基础框架收口

在实现 Markdown、TOTP、代码预览或脚本能力前，先完成以下基础能力；本阶段不加入任何具体内容处理功能：

1. 为画布节点建立真实 DOM 组件测试，覆盖长文本光标、Enter、Shift+Enter、Backspace、节点内部焦点切换和失焦提交，防止内容宿主接入后恢复旧的输入跳转与错误提交问题。
2. 建立显式的工作区格式迁移链。现有 v1 工作区读取后迁移到当前版本，新保存、导出和恢复只写当前版本；迁移必须是确定性的，并由旧版夹具测试覆盖。
3. TypeScript 与 Rust 使用同一组 JSON 合约夹具验证合法工作区、重复名称、悬空引用、缺失布局、非法视口和版本迁移，避免两端验证规则继续漂移。
4. 内存存储与 D1 统一使用“规范化名称、节点 ID”作为分页稳定顺序；中英文资源加入递归键集合一致性测试。
5. 建立共享的 `ContentProcessor` 注册表和 `NodeContentHost`。画布与列表不再各自解释正文；纯文本处理器是唯一默认实现，未知处理器降级为纯文本且保留原文。

内容处理器选择记录属于工作区级视图元数据，必须与节点、引用和布局一起进入加密保存、撤销、导出、历史和异机快照。它不属于 `InformationNode` 或 `Reference` 领域模型，也不得另存到未加密的浏览器存储。只保存非默认处理器的选择，删除节点时必须同步删除对应选择。

视图元数据使用开放的处理器标识符。读取到当前版本不认识的标识符时，应用必须保留该标识符和原始正文，以纯文本安全显示并允许用户改回已安装处理器；不能因为当前版本缺少插件而在下一次保存时静默删除元数据。

实时派生状态由处理器宿主局部持有。TOTP 倒计时、预览刷新或代码高亮不得放进顶层 `App` 并触发整张画布每秒重建。Markdown 默认禁用原始 HTML并经过严格净化；TOTP 密钥不得进入日志、向量缓存、模型输入或普通剪贴板；脚本执行仍属于独立安全阶段，只有代码预览可以在内容处理器阶段实现。
