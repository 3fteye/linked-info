[package]
name = "linked-info-application"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
linked-info-domain = { path = "../domain" }
linked-info-storage-port = { path = "../storage-port" }

[dev-dependencies]
linked-info-storage-memory = { path = "../storage-memory" }
tokio = { version = "1", features = ["macros", "rt"] }
