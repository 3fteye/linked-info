# 仓库协作说明

本文件为 Codex 普通 Code Review 提供仓库级规则。安全审查使用仓库单独配置的 [SECURITY.md](SECURITY.md)，不在这里复制安全规则。

实现前先阅读 [DEVELOPMENT.md](DEVELOPMENT.md) 中与改动范围对应的架构说明；产品决策和当前阶段以 [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) 为准。

## 提交、发布与云端审查

任何 AI、agent、编辑器助手或自动化工具在被要求暂存、提交、推送、创建或更新 PR、起草或改写 commit message、审查 staged changes、选择提交拆分点、处理云端 Review，或修复历史 commit message 时，必须完整读取并执行 [.agents/skills/linked-info-commit-discipline/SKILL.md](.agents/skills/linked-info-commit-discipline/SKILL.md)。

## Code Review Rules

### 数据兼容性

- 持久化工作区、导出、导入、备份或恢复格式发生变化时，必须保留明确的版本边界、严格入口校验和已存在数据的迁移或拒绝策略。不能让旧数据被静默解释成空工作区或部分工作区。
- 节点、引用和布局必须继续满足一一对应、端点存在、标识符唯一及非空名称规范化后唯一等仓库不变量。格式转换必须在边界完成，不能让两种文档结构在内部混用。
- 会替换整份工作区或产生批量修改的操作必须保持原子提交和一次历史事务语义；取消、失败或过期结果不得留下部分修改。

### 架构边界

- 领域层和供应商无关端口不能依赖 Tauri、React、Cloudflare、S3 或具体模型 SDK。新的提供者通过现有端口和适配器接入，不能把供应商类型传入领域模型或视图。
- 画布、列表和其他视图共享的数据解释逻辑应进入已有宿主或注册表；不能在多个视图中分别实现同一种内容语义。
- 界面文案必须同时加入中文和英文资源，不能在 React 组件中新增直接面向用户的硬编码字符串。

### 性能与交互

- 评估改动在大型工作区上的复杂度。不能把完整长正文、全部引用边或每秒更新的派生状态重新注入顶层 `App` 或整张 React Flow 画布。
- 拖动、平移、缩放、框选和连线等高频路径不能在每个指针事件中执行全图扫描、持久化或同步布局测量；需要全图计算时应放到明确的完成边界或独立 Worker。
- 自动布局、批量导入和整工作区恢复必须保持可取消、可预览或可撤销的既有语义，不得通过优化删除用户确认边界。

### 回归验证

- 修复可复现缺陷时必须增加能够在修复前失败的最小回归测试。交互问题优先使用合成工作区的 DOM 或 Microsoft Edge 测试，不能依赖真实用户工作区。
- 测试、格式化、许可证和依赖审计交给 CI；普通 Code Review 聚焦 CI 不容易发现的行为错误、兼容性破坏、架构倒退和明显性能回归。
- 只根据当前改动提出可执行问题。缺少证据的未来扩展建议、纯风格偏好和已经由确定性检查覆盖的问题不作为阻断项。
