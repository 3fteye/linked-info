# 安全策略与威胁边界

本文说明“关联信息”对秘密数据作出的安全承诺、明确不覆盖的威胁，以及安全相关改动的发布要求。实现架构见 [DEVELOPMENT.md](DEVELOPMENT.md)，开发顺序见 [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md)。

## 当前安全状态

- 未启用工作区加密时，节点、引用、布局、恢复副本和本机历史以明文保存在当前设备，不适合保存密码、令牌、恢复码或 TOTP 秘钥。
- 启用主密码后，正式工作区、恢复副本、本机历史和默认导出使用客户端认证加密；错误密码或密文篡改不能产生部分工作区。
- 主密码是可移植的根解锁入口。Windows“系统快速解锁”只是当前设备入口，不进入导出、备份或同步，也不是第二因素。
- Rust 已实现默认 15 分钟闲置锁定、Windows 会话/休眠锁定、异步任务代次失效，以及修改主密码、完整/原始导出和关闭系统快速解锁前的限定用途单次重新认证。
- 正式 WebView 已启用严格 CSP；主窗口不再拥有前端通用文件读写权限，导入导出由 Rust 在用户逐次选择的路径上执行。
- 悬浮胶囊已写入独立窗口、主 owner 和 Rust 命令隔离实现；胶囊不加载整库，未提交草稿仅驻留内存。当前胶囊改动尚未运行 CI，原生 Windows 失焦、系统锁定/休眠和退出行为也未完成实机验收，不能据此宣称桌面安全边界已验证完成。
- 设置页可以在重新认证后删除全部自动历史和恢复副本而保留当前工作区，也可以在严格锁定后销毁整个工作区和设备快速解锁入口。独立的数据密钥轮换会先锁定，再重加密正式工作区、恢复副本和本机历史，并替换当前设备快速解锁凭据。
- 本机自动历史受 30 份、512 MiB 和 90 天三重上限约束。Windows 加密工作区提供显式“复制内容（按秘密处理）”，在剪贴板未变化时 45 秒后清除，并在锁定、密钥轮换和正常退出时提前清除。
- 桌面端已经具备可选的异机密文备份适配器和手动上传、完整下载校验、恢复预览入口，但默认仍只使用本地文件；仓库没有替用户部署 Cloudflare 资源，也不会自动连接任何远端。现有 Worker/D1 节点 API 不属于秘密数据备份路径。
- 当前开发构建没有商业代码签名和稳定自动更新通道。

在删除留存、数据密钥轮换、发布供应链和真实桌面安全行为完成验证前，项目不能把“已加密”描述为完整的密码管理器安全能力。

## 威胁模型

### 目标内

- 工作区文件、恢复副本、历史或导出文件被单独复制后，攻击者不能在不知道主密码的情况下读取内容。
- 密文被修改、截断或使用错误密钥解密时快速失败，不用空数据或部分数据覆盖现有文件。
- 应用锁定后，新的明文读取、导出、模型分析和内容处理请求必须在 Rust 边界失败。
- 前端内容或依赖受到攻击时，CSP、Tauri capability 和 Rust 命令授权共同限制其加载远程代码、任意联网、访问文件和读取解锁数据的能力。
- 远端备份提供者只收到客户端已经加密的不透明快照，不取得主密码、数据密钥、节点名称、正文、引用或布局。
- 发布产物可以追溯到固定源码提交和依赖集合，并能验证文件未在发布后被修改。

### 目标外

应用解锁后，不能抵御已经取得当前用户或更高权限的键盘记录器、屏幕读取、进程注入、调试器、管理员恶意软件、内存转储或被攻破的操作系统。系统剪贴板历史、云剪贴板和用户主动粘贴到其他程序也不受工作区加密保护。用户应启用 BitLocker、FileVault 或同类全盘加密，并维护独立的离线或异机加密备份。

认证加密能够发现密文内容被修改，但不能单独发现攻击者把整个文件替换成较旧、仍然合法的密文。只有远端同步或恶意回滚成为实际威胁时，才增加单调版本和设备或远端锚点。

## 强制安全边界

### 锁定与明文任务

- 锁定决策和解锁数据密钥由 Rust 持有，不能只隐藏 React 界面。
- Windows 会话锁定、休眠和用户切换触发立即锁定；加密工作区支持 5、15、30 分钟及关闭闲置锁定，默认 15 分钟。
- 用户输入、画布操作、受信任的本地命令可以刷新闲置期限；模型下载进度、网络响应、动画和后台定时器不能被视为用户活动。
- 锁定先原子撤销明文操作权限并推进任务代次，再停止或清理模型、令牌、缓存和界面。手动或闲置锁定、数据密钥轮换和正常退出必须在撤销后、任何可能等待在途模型或扩展任务的清理之前，条件清除仍由应用跟踪的秘密剪贴板。permit 签发与验证、数据密钥替换和锁定撤销必须在同一把 Rust 数据密钥锁下完成代次与密钥状态转换，不能签发或接受“新代次仍绑定旧密钥”的授权。React 安全门必须锁存 Rust 锁定事件并拒绝迟到或乱序的“已解锁”状态探测；只有显式解锁成功可以清除锁存。保存或进程清理失败不得使工作区继续保持已授权状态。
- 显式恢复确认是受保护的例外：Rust 会在普通锁定清理待提交恢复前，按用户确认的 `restore_id` 原子取出已验证载荷并交给同一事务；错误或过期 ID 不会触发锁定，也不会消耗其他待提交恢复。未确认的恢复准备仍会在普通锁定时清除。
- 锁定前启动的异步任务必须携带任务代次；代次失效后不得返回明文结果、写入引用或刷新用户活动。不能安全中止的进程任务应隔离到可终止的子进程。
- 本地 LLM 和嵌入任务还有独立的 admission gate。锁定或退出会取消并等待在途任务后再释放模型；模型哈希的 `spawn_blocking` 返回后仍需复验取消，取消不得写校验标记、发布 `Ready` 或返回结果。哈希线程不能强杀时，Rust 会等待其完成而不重新开放准入。

### 敏感操作重新认证

以下操作即使工作区已经解锁，也必须在 Rust 边界要求当前主密码或一次新的系统用户验证：

- 修改主密码。
- 启用或关闭系统快速解锁。
- 完整导出工作区或不可读原始数据。
- 清除本机历史、恢复副本或销毁整个工作区。
- 数据密钥轮换，以及将来的设备授权和备份凭据变更。

重新认证产生的是短时、限定用途的授权，不是新的长期解锁入口；React 不保存当前主密码，也不能自行判断重新认证成功。

### WebView、CSP 与 IPC

- 正式构建使用严格 CSP：打包脚本和资源默认只允许来自应用自身；禁止远端脚本、iframe、object、任意表单提交和未列入白名单的 WebView 网络连接。
- 开发服务器所需的热更新权限只能进入开发配置，不能放宽正式构建。
- 远端备份和远端 AI 网络请求由 Rust 适配器执行；WebView 不因新增提供者而获得任意 `connect-src`。
- 文件导入导出通过一次性的 Rust 命令完成。主 WebView 不持有通用文本文件读写权限。
- Tauri capability 只授予各窗口实际需要的权限；胶囊仅有事件监听权限，不具有通用文件、导入导出、密码或模型权限。Rust 的自定义命令入口同时检查真实窗口与 WebView label，并对胶囊使用专属命令白名单，不能把 capability 当成自定义命令天然隔离的证明；命令仍须执行锁定状态、用途和参数校验。
- 富文本默认使用禁用危险 HTML 的 Markdown 展示；节点内容不能向 WebView 注入脚本。脚本处理器必须在独立、可终止且默认无文件、网络、环境变量和其他秘密访问权的进程中运行。

### 悬浮胶囊的所有权、草稿和提交

- 正式工作区的唯一前端写入者是主窗口 owner；Rust 固定绑定随机 owner 身份、原始 permit 和访问代次，明文工作区也检查代次。胶囊只提交有界录入请求，不接收整份工作区，不持有独立工作区保存队列。请求还绑定可撤销的 context，接纳、取出、落盘和回执均复验；旧 owner、旧 context 及卸载 flush 不得重新签发权限后写入新会话。
- 未提交草稿只留在当前进程内存，不建立明文持久化暂存区；Rust 最多保留一条有界请求，完成后的收据保留身份与指纹并释放正文。收起保留草稿并提示未保存，有草稿或未解决提交时禁用界面隐藏按钮；锁定、工作区替换和退出清理旧草稿。加密工作区锁定时仅提供前往主窗口解锁的入口，明文模式必须提示内容按明文保存。
- 系统锁定和休眠先撤销 owner、请求权限及访问代次，不等待胶囊或完整文件保存。已经获准的在途写入可能完成，但新 owner 在同一文件操作队列之后激活；已提交且会话过期的结果不能反报未保存。胶囊提交没有关机补保存承诺，关闭主窗口也不转为后台驻留。
- 未加密工作区显式启用加密或初始化恢复时，Rust 同样立即撤销 owner 与胶囊；主窗口已冻结在独占事务内，因此仅抑制这两种明文事务的提前自动重建事件，等待明确结果。该例外不适用于加密工作区、系统锁定或休眠，也不重新授权任何旧写入。
- 只有持久化确认后才能显示保存成功。`capsule_commit_not_saved` 仅代表明确替换前失败；未知提交错误不得自动换身份重试。写入已发生但回执或目录持久性不确定时必须失败关闭，禁止旧快照覆盖可能已经落盘的结果；Rust 的 `recoveryRequired` 隔离不能通过同进程重新打开 owner 或刷新 WebView 绕过，需真正重启应用进程检查。仅主窗口可调用原生 `restart_application`，先复用退出的撤权、秘密剪贴板及模型/扩展清理，再重启；胶囊不能调用该命令。

### 第三方扩展隔离

- 第三方扩展只能以 WebAssembly Component 在独立 `linked-info-extension-host` 子进程中运行；主 WebView 不加载扩展 JavaScript、HTML、CSS 或原生库，桌面主进程也不链接 Wasmtime。
- 扩展包在子进程中重新执行签名、哈希、清单、Schema、容量和 Component 校验。WIT 的纯数据类型直接属于导出的 `guest` 接口；运行时使用空 Linker并拒绝全部组件导入。第一版不提供通用 WASI、文件、网络、环境变量、命令行、系统时钟或随机源。
- 所有请求和返回值都经过版本化、有帧容量上限的结构化 IPC、清单能力复验、临时句柄校验、元数据 Schema 校验和声明式 UI 校验。扩展输出不能直接成为 HTML，也不能直接写节点、引用、布局或工作区文件。
- 普通内容读取能力不包含已标记秘密。桌面 Rust 在跨进程前再次剥离 `secret`、`totp` 和旧式 TOTP payload；原始秘密读取不得通过普通扩展能力开放。未标记文本仍按用户授予的正文读取能力处理，应用不会猜测所有类似密钥的字符串。
- 每次调用使用新的 Store 和实例，并受 128 MiB 线性内存、实例/表数量、fuel、epoch、墙钟、输入和输出上限共同限制。宿主进程另受 512 MiB 总内存上限，以覆盖不属于来宾线性内存的编译器和 JIT 开销；子进程只有在父进程或自身安装该限制后才开始读取不可信包。宿主崩溃、陷阱、超时、超限、错误输出或来宾错误信息都只能产生结构化失败，不得把来宾错误正文写入普通日志。
- 锁定、休眠、工作区替换、加密启用和数据密钥轮换先从宿主表移除全部扩展并通过独立操作系统终止句柄立即杀死子进程。普通调用先在主进程内剥离秘密并准备有界帧，再以极短的“代次复验—请求准入”门控与撤销排序；实际 IPC 写入、来宾执行和响应等待均在门控外。已准入但仍在途的管道 I/O 由独立终止句柄中断，不能阻塞锁定；授权代次用于拒绝迟到结果，但不能替代进程终止。

### 密码与密钥生命周期

- 新建和修改主密码至少 15 个 Unicode 字符，允许空格和长密码，不要求无意义的字符种类组合。
- 使用本地阻止列表拒绝常见或已泄露密码；密码不得发送到远端检查。
- Argon2id 参数带版本保存，并在目标设备上建立可接受耗时基线；参数升级只重新封装数据密钥。
- 连续解锁失败使用 Rust 侧递增等待。该限制只降低在线尝试速度，不能阻止复制密文后的离线破解。
- 普通改密码只重新封装现有数据密钥；怀疑设备或会话泄露时使用独立“轮换数据密钥”，原子重加密正式工作区、恢复副本和本机历史。
- 轮换先撤销当前明文会话并立即通知界面卸载明文状态，再在独立事务目录中准备、验证全部新密文；缓存清理、线程启动和文件轮换的任何终态都必须发送成功或失败事件，不能因中间错误留下“Rust 已锁、界面仍解锁”的分裂状态。vault 元数据最后提交。进程中断后只允许丢弃未准备完成的事务或完成已就绪事务，不允许在旧、新密钥混用的状态下解锁。轮换成功或失败后都保持锁定。
- 当前设备启用了系统快速解锁时，轮换同时创建新的设备凭据，并在新 vault 提交后删除旧凭据。用户另存到应用目录之外的导出文件是独立副本，不会被轮换。
- vault 提交后如果旧设备凭据或事务目录暂时无法清理，状态会保留为 `committed_cleanup_pending` 并在下次安全状态检查重试；新 vault 仍可启动和解锁，不把提交后的清理故障报告为轮换未发生。
- 如果清理阶段标记本身写入失败，只有在正式 vault 与待提交 vault 字节一致、能够证明提交点已经发生时才放行新 vault；标记和旧凭据清理随后继续重试。跨平台恢复不会把旧系统的 credential ID 交给当前平台删除，冗余轮换目录清理后报告 `CleanupSkipped`，旧凭据需在原设备处理。无法证明提交点的 `ready` 事务继续阻断，避免把未提交数据误当正式 vault。

### 删除、剪贴板与日志

- “从当前工作区删除”“清除本机历史与恢复副本”“销毁整个工作区和设备入口”是三个不同操作，界面必须明确影响范围。
- 不宣称能够安全擦除 SSD 单个文件。整个工作区销毁以删除所有数据密钥封装和设备入口为密码学边界；用户仍需依赖系统全盘加密处理存储介质残留。
- 当前销毁命令要求主密码或新的系统用户验证和明确确认短语；Rust 先撤销明文访问并卸载工作区，再清理向量与智能引用缓存、删除设备快速解锁凭据、正式文件、待提交文件、恢复副本、自动历史和 vault。任一步失败都保持锁定并报告失败。应用管理范围之外的手动导出不在销毁范围内。
- 专用“复制秘密”只在加密工作区显示。Windows 适配器记录写入后的剪贴板序列号，45 秒后仅在序列未变化时清除；锁定、数据密钥轮换和正常退出会提前尝试同样的条件清除。普通文本复制不自动清除。系统剪贴板历史、跨设备同步、应用或系统崩溃仍可能保留副本，界面必须在每次复制后提示这一边界。
- 智能引用持久缓存的跨语言 DTO 必须包含同一份 `sourceFingerprint` 字段（64 位小写 SHA-256）；缺失或不合法条目按损坏缓存删除，不能被当作有效分析结果。缓存仍不得保存节点正文、秘密载荷或模型请求。
- 节点名称、正文、主密码、令牌、TOTP 秘钥和验证码、恢复码、模型输入、脚本输入输出不得进入普通日志、遥测或上传的崩溃转储。
- CI 对直接接触上述数据的 Rust 与 TypeScript 模块执行源码门禁，禁止 stdout/stderr 调试宏、普通 Rust 日志 facade 和浏览器 console 日志。该门禁防止直接日志调用，不代替将来对第三方崩溃报告、系统转储和数据流的审查。

## 备份与远端提供者

- 目标配置与系统密钥环之间使用带 HMAC 的持久化事务日志协调创建、更新和移除。消费重新认证授权的配置变更必须先取得配置写锁，再在数据密钥线性化边界完整复验原 permit；锁定先发生时不得进入 mutation，准入先发生时由事务日志完成已经获准的提交。正式配置提交后旧凭据清理失败只进入可重试状态，不能把已提交变更报告为未发生，也不能覆盖未完成日志。已提交的新建目标遇到确定的系统凭据 `NoEntry` 时清除事务日志并保留可修复的目标配置；暂时读取失败时保留日志重试，不能把系统安全存储暂时不可用误判成凭据永久缺失。
- 配置、事务日志和旧格式迁移 intent 的原子写以文件替换成功为提交点，并把父目录持久化确认失败结构化为 `recoveryRequired`；普通错误只能表示尚未替换。恢复状态必须在仍持有配置写锁时锁存到 Rust 进程生命周期闩，后续本机配置写、远端上传、远端删除和自动保留均失败关闭；列表、下载、恢复预览等只读操作仍可使用。破坏性分页操作在每一步同时复验工作区 permit 与该闩。界面不得乐观更新目标或把已经完成的远端动作说成失败，必须重新读取磁盘权威状态、清除表单与恢复演练中的秘密，并暂停本会话的备份修改直到应用重启。
- 从旧未认证目标配置升级时，先原子写入包含目标配置且由新 HMAC 密钥认证的迁移 intent，再激活系统安全存储中的密钥，最后替换认证配置并清理 intent。已有认证密钥却没有匹配的有效 intent 时仍拒绝旧未认证配置，不能为了崩溃恢复而重新信任可被替换的明文文件。

本机自动历史、导入前恢复副本、手动导出和异机备份是四种不同生命周期。异机备份优先实现追加式加密快照和显式恢复，不在第一版实现多设备双向合并。

供应商无关的 `BackupTarget` 边界至少提供上传、列举、下载、删除和验证。每个目标独立保存最近成功备份、最近验证和最近恢复演练时间；服务商凭据由当前设备的系统安全存储保护，不进入工作区、导出或其他备份目标。

所有对象存储目标统一通过桌面 Rust 中的 S3 兼容适配器访问。Cloudflare R2、Backblaze B2、Tigris、Oracle OCI 和自定义 S3 只提供连接参数模板，不拥有独立协议实现。服务商访问密钥必须限制到专用存储桶的对象读写权限；服务商侧静态加密不能替代客户端加密。

桌面端可以登记多个目标、手动上传快照、列举版本、完整下载并重新计算哈希，再把下载结果交给加密导入与画布差异预览流程。没有本地 vault 的设备也可临时输入 S3 端点、区域、存储桶、前缀和访问密钥，下载快照并仅凭创建快照时的主密码准备恢复；Rust 只保留五分钟的待确认数据密钥，确认画布差异后原子建立本地 vault 和加密主工作区，原本地明文工作区尽可能转为加密恢复副本，系统快速解锁入口不随快照迁移。临时连接信息不会自动登记为目标。仓库本身不会替部署者创建对象存储资源或保存账户凭据。

远端快照必须使用可由主密码在全新配置中独立解开的加密导出封装，不能直接复制只能依赖当前进程内数据密钥的本地工作文件。供应商只允许看到随机快照标识、创建时间、密文字节数、内容哈希和访问频率；节点数量、名称、正文、引用、布局、主密码、数据密钥、系统快速解锁凭据以及其他备份目标凭据都不得上传。文件大小、时间和版本次数仍然是无法隐藏的元数据泄露，界面必须明确说明。

桌面端只接受没有内嵌用户名、密码、查询参数或片段的 HTTPS S3 端点，并禁止自动跟随重定向。Access Key ID、Secret Access Key 和可选会话令牌作为一个版本化凭据记录保存在操作系统安全存储中；本地目标配置只保留凭据 UUID，不得保存密钥副本。列表和下载密文都设置硬上限，避免恶意或错误端点用无限响应体耗尽内存。

已登记目标支持原地修改和访问密钥轮换，但任何秘密都不会从系统安全存储回显到 WebView。候选连接必须先完成远端列举验证并通过当前主密码或系统快速解锁重新认证；验证或配置写入失败时，当前目标及其凭据保持不变。新凭据使用新的凭据 UUID，带 HMAC 的目标配置成功切换后才清理旧凭据。只更换凭据不改变目标身份或既有验证状态；改变端点、区域、存储桶或前缀会使既有上传、完整校验和恢复演练结论失效，同时关闭自动远端清理，直到在新位置重新完成闭环。

每份加密快照保存创建当时的主密码封装。修改当前工作区主密码不会重写已经位于异机的历史快照，因此旧快照可能仍要求旧主密码；数据密钥轮换在主密码不变时不影响旧快照的密码。用户界面必须披露这一点，恢复演练需要实际使用相应密码完成解密，而不能只验证 R2 对象哈希。

设置页的“恢复演练”不替换当前工作区。Rust 完整下载并验证快照后，在随机且原先不存在的系统临时目录中建立新的加密工作区，确认设备快速解锁入口没有迁移，再使用该快照的主密码重新取得数据密钥并核对完整工作区。只有验证成功且临时目录删除成功后才记录该目标的最近恢复演练时间；错误密码、损坏快照、安装失败或清理失败都必须报告失败。

自动远端保留必须失败关闭。只有恢复演练时间与对应快照 ID 同时存在，且该快照仍出现在清理前取得的完整远端列表中，才允许发出删除请求。目标位置修改、恢复演练、保留规则修改、上传后的清理和快照删除按目标串行；删除演练快照必须尝试同时清空证明并关闭保留。远端删除一旦确认提交，后续会话锁定或本地状态写入失败不能把它伪报成远端删除失败；本地写入失败必须返回明确告警，而缺失的证明快照仍会让后续自动清理在任何删除前停止。旧配置中只有时间而没有快照 ID 的记录仍可读取和关闭规则，但不能启用或执行清理。

恢复演练不能把所有密钥认证失败都显示成“用户输错密码”。当快照主密码解封失败时，Rust 会在不返回明文或密钥的前提下继续验证：输入是否能解锁当前工作区、快照正文是否仍属于当前内存数据密钥、快照封装是否与当前 vault 完全一致。界面必须分别报告“输入不是当前主密码”“快照使用不同的密码封装但正文密钥相同”“快照属于不同数据密钥世代/其他工作区或已损坏”和“不可能的当前封装不一致”。该诊断只解释当前设备上的失败原因，不能把依赖当前内存密钥的成功误记为全新设备恢复成功。

实际 Cloudflare R2 目标已经在 Windows 桌面构建中使用真实加密工作区快照完成一次上述恢复演练，并成功记录最近恢复演练时间。成功后密码对话框会原位切换为明确的通过结果，用户确认关闭后才退出，不能仅通过对话框消失暗示成功。

通用 S3 兼容目标直接连接用户提供的对象存储，不经过项目部署的 Cloudflare Worker。端点、区域、桶和对象前缀属于非秘密配置；Access Key ID、Secret Access Key 和可选 Session Token 作为同一份版本化凭据保存在系统安全存储中，不进入工作区、普通配置、导出或其他备份目标。请求使用 AWS Signature Version 4，强制 HTTPS、path-style 寻址且不跟随重定向。提供商仍可看到桶、对象键、密文大小、时间和访问频率。

S3 快照对象键包含版本、创建时间、随机 UUID 和客户端 SHA-256，以便只用 `ListObjectsV2` 重建列表；键中的哈希只是待核对值。每次完整校验、恢复预览和恢复演练仍必须下载全部密文并在本机重新计算 SHA-256。不符合应用前缀和键格式的桶内对象不会展示或删除。

多目标备份复用同一份版本化密文快照，但分别报告失败和验证状态。至少一次恢复演练必须在全新的应用配置中完成，证明主密码可以恢复全部节点、引用和布局且不依赖原设备快速解锁入口。

破坏性删除另用 `ListObjectVersions` 检查所有对象版本和删除标记，并使用带 `versionId` 的删除请求；服务商不支持或无法证明完整清理时返回未验证结果并保留本机目标。版本列表为空的回退探测必须检查应用前缀下的原始对象，畸形键不能被普通浏览列表过滤掉后误报为已清理；缺失分页游标也必须 fail-closed。删除流程在每页、每个版本请求和最终复核前后检查工作区完整 permit 与解锁状态，不能只比较访问代次；已发出的单个请求可能完成，但锁定后不得开始后续破坏性步骤。界面字段 `deletedVersionCount` 表示版本/删除标记数量，不是快照数量。

## 发布与供应链

- CI 已对 Rust `Cargo.lock` 执行 RustSec 审计并对生产前端依赖执行高危阈值审计。依赖许可证必须经过仓库内精确允许列表审查；新增或缺失许可证默认阻断。Rust workspace 各 crate 与桌面前端生产依赖分别生成 CycloneDX SBOM，二者不能代替漏洞审计或法律审查。
- 当前 RustSec 报告包含由跨平台桌面依赖带入的无人维护传递依赖，以及 Linux GTK3 路径的 `glib 0.18` 不健全告警。它不进入当前 Windows 构建路径，但在宣称支持 Linux 发布前必须通过升级桌面依赖或移除旧 GTK3 路径解决，不能静默忽略。
- 模型、sidecar 和构建时下载文件固定上游提交与 SHA-256；只校验长度不构成完整性验证。
- 第三方 GitHub Actions 固定到经过审查的完整提交 SHA。
- 当前便携构建生成 SHA-256，并随构建保存 Rust 与前端依赖 SBOM；公开发布前还必须补充构建来源证明。来源证明不能替代操作系统代码签名。
- 当前自用开发构建允许未签名。向普通 Windows 用户公开发布前，EXE、安装包和更新包必须使用一致发布者身份的 Authenticode 签名和可信时间戳；自动更新必须验证签名。

## 漏洞报告

项目尚未建立专用安全邮箱。在公开接收外部用户之前必须补充私下报告渠道。不要在公开 Issue 中提交真实工作区、密码、令牌、恢复码、TOTP 秘钥、密文样本或设备凭据。
