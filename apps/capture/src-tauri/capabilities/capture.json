{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture",
  "description": "Independent plaintext inbox; no workspace, file, model, or key access",
  "windows": ["capture"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
