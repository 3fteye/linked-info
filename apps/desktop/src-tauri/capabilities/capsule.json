{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capsule",
  "description": "Bounded capture window; custom commands use a Rust window-label allowlist",
  "windows": ["capsule"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
