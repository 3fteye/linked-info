# Backlog

当前便签优先项已经从同进程胶囊改为[独立明文便签应用](docs/standalone-capture.md)，按持久收件箱、主应用归档、独立界面与打包三个切片推进。无需密码或公钥封存；主工作区加密边界不变。

本文档记录主体框架完成后的候选能力、外部验收和明确后置项。原同进程胶囊随 PR #16 合并并更新本机固定入口；旧版自动验证只属于[历史胶囊记录](docs/capsule-notes.md#验证记录)，不代表独立程序已完成验证。真实操作系统锁定/休眠、Windows Hello 等人工验收仍保留。

## 当前实施方向（2026-09-03）

- 提供一个可折叠为胶囊、按需展开的悬浮便签入口，用于随时记录短期信息。一条记录形成正式保存时，应用按当时系统时间创建正式节点，并把它放入系统管理的内置时间画布。
- 日期在当天首次记录时生成：便签引用当天日期，日期按先后单向连接，不串联所有便签。失焦或 `Ctrl+Enter` 进入本机队列，主应用解锁后归档；只为新节点安排位置，不重排旧布局。
- 交接契约、持久化状态和失败恢复集中在[独立明文便签基线](docs/standalone-capture.md)。主 owner 仍是唯一正式工作区写入者，便签不加载整库；本地保存与正式归档分开确认。
- 外部文档导入扩展、远端同步和脚本执行已明确不属于当前主线。它们保留为后置边界，不得与本轮便签设计混合实施。

## 智能引用后续能力

- 只有实际使用证明现有候选排序不足时，才评估小型重排模型；不得仅因为模型存在就增加部署体积和推理耗时。
- “智能引用操作记录与批量撤销”不是拿用户数据训练或微调模型。它只是可能有需求时再实现的设备本地操作记录，保存某次分析使用的算法/模型指纹、建议的候选 ID、最终接受或拒绝的候选，以及由该次分析自动新增的引用。
- 操作记录的第一用途是解释和按一次分析撤销自动修改；第二用途才是用本地统计比较算法版本。任何把这些记录用于训练、上传或跨用户聚合的能力都必须另行设计并由用户明确开启。
- 后台批量分析、定时重新索引、批量接受或撤销自动引用，需要先补齐任务进度、取消、失败恢复、费用上限和上述操作记录，再单独设计。
- 操作记录属于设备本地派生数据，不修改 `Reference` 核心模型；密码保护工作区中也必须加密，默认不导出、不同步。

## 搜索与关系浏览

- 保存、命名和复用常用筛选组合尚未形成实际需求；只有手动重复配置筛选成为明确负担时再评估。

## 内容展示与本地处理

- 脚本执行已明确后置；将来重新启用时必须单独设计权限、隔离、超时、取消、输入和输出边界，不能把节点内容直接交给 WebView 或系统 shell 执行。

## 桌面便签与时间视图

- 当前方向是独立明文便签先记录，主应用解锁后按原时间归档；不依赖关机补存。主应用锁定只阻止正式归档，不阻止便签自身的本地录入。
- 草稿和待归档队列持久保存，两个应用各自退出；主应用锁定、替换、退出不能清空便签。便签明确提示明文边界，不提供主工作区密钥或通用文件权限；不增加托盘和自启。
- 时间画布是系统管理的内置画布，不是新的领域实体或扩展私有容器。它继续复用正式 `Node + Reference`，按自然日首次有记录时生成日期节点；复用、跨日串联、记录归属、布局及删除规则见实施基线。
- 本轮必须重新验证收件箱、独立进程与归档生命周期；旧版 14 项原生结果不替代新验证。本轮尚未授权合并或更新安装，不得读取真实秘密工作区或通过截图代替验收。
- 已有自动排版测试波动待单独定位：`smart arrangement normalizes width and saves one undoable layout step` 在本轮 Edge CI 出现 5 秒 predicate 超时，重试通过；`CI 33737879321` 同一用例另出现刷新时 `Execution context was destroyed`，也在一次重试后通过。登记后独立调查等待条件和导航时序证据，不以放宽超时替代根因定位，也不扩大本轮胶囊代码范围。

## 架构审查后续基线（2026-08-30）

本节记录用户提供的仓库级静态审查（固定基线 `main@1743de8`）经过实现、自查和云端复审后的结论。PR [#15](https://github.com/3fteye/linked-info/pull/15) 已于 2026-08-31 squash 合并为 `main@2b41083`；[Windows CI 33354555574](https://github.com/3fteye/linked-info/actions/runs/33354555574) 和 [Desktop packages 33354594566](https://github.com/3fteye/linked-info/actions/runs/33354594566) 均通过，PR 最终 head `5e4fd20` 的普通与安全审查没有新的未解决发现。自动化和审查通过仍不替代真实服务、系统密钥环和目标平台生命周期验证。

### 必须保留

1. **数据密钥轮换的提交后清理（P1）**：已拆成 `CommittedCleanupPending` 状态；vault 提交后旧系统凭据或事务目录清理失败只保留可重试状态，不阻断新 vault 的启动和解锁。显式恢复会在普通锁定清理待提交状态前按 `restore_id` 原子取出已验证载荷，避免确认恢复被误判为未准备。对应故障注入回归已随 Windows CI 33308723865 通过。
2. **扩展锁定竞态（P1，安全边界）**：托管扩展现在贯穿使用访问许可代数，启动、写入 IPC 和响应返回均复验同一代数；锁定先推进运行时撤销边界再清除 vault，并有 barrier 回归测试。对应自动化回归已随 Windows CI 33308723865 通过。
3. **离站备份配置跨介质事务（P1）**：创建、更新、移除现在使用带 HMAC 的持久化事务日志；配置提交和系统密钥环清理可在下次读取时恢复，提交后清理失败返回明确的待重试状态，且不会覆盖未完成日志。对应自动化回归已随 Windows CI 33308723865 通过，真实系统密钥环故障仍待目标平台验证。
4. **加密启用与向量缓存写竞态（P1，安全边界）**：缓存读写在取得锁后再次验证加密状态和缓存代数；加密迁移前后推进代数并清空缓存，迟到写入会被丢弃。对应 barrier 回归已随 Windows CI 33308723865 通过。
5. **版本化 S3 桶的远端删除语义（P1）**：`BackupTarget` 已增加版本删除/全量清理契约；S3 适配器枚举 `ListObjectVersions` 的对象版本和删除标记，带 `versionId` 删除并复核为空。不能证明完整删除时返回 `Unverified`，桌面端保留目标配置。待真实版本化桶验证。
6. **离站配置原子替换后的提交判定（P1）**：已使用 `Committed / RecoveryRequired` 区分替换点和父目录持久化确认；配置事务、旧格式迁移、全部本机状态写和远端写门禁共享同一 Rust 恢复闩，前端重新读取权威状态并丢弃迟到自动结果。替换前后、排队写、迁移重启和远端 guard 回归已随 Windows CI 33352139368 通过。

### 条件性保留

7. **Cloudflare Worker 身份与工作区隔离（远端启用前 P0 发布闸门）**：Worker 当前仍匿名暴露全局节点/引用 CRUD，D1 表没有主体或工作区维度。桌面端目前不调用它，文档已明确禁止直接公开部署，因此暂不引入简单共享 Token；如果保留 Worker 作为公开或同步后端，必须先设计经过验证的主体、工作区作用域、数据库约束和越权集成测试。
8. **D1 创建引用的原子性（P2；仅在继续维护 Worker API 时）**：已改为 D1 原子 batch，在同一事务快照内以 `INSERT ... SELECT ... WHERE EXISTS` 检查端点，并把重复引用与端点不存在区分开。仍缺真实 D1 并发集成测试。

### 已发现的补充边界

- 智能引用结果缓存的 Rust/TypeScript DTO 现在都要求 `sourceFingerprint` 为 64 位小写 SHA-256；旧的缺字段条目按损坏缓存丢弃，不得当作有效结果使用。
- 版本化 S3 的破坏性删除在版本 API 不可用、分页无法验证或应用前缀下出现任何无法解析的对象时返回 `Unverified`；不能因为普通列表过滤了畸形键就报告“已删除”。
- 向量缓存和本地 LLM 使用有界代数与取消复验。代数达到 `u64::MAX` 时拒绝新写入/迁移，不回绕；模型哈希在 `spawn_blocking` 返回后仍需检查取消，取消不得发布校验标记或 `Ready` 状态。
- 数据密钥轮换若已确认正式 vault 已提交，即使提交后清理标记暂时写不进去也放行新 vault；下次恢复继续重试标记、旧凭据和事务目录清理。若无法证明 vault 已提交，仍必须阻断并要求恢复。

### 自查判断

- #1 和 #5 已有文档披露或限制，但文档不能替代代码中的认证、作用域和删除完成语义，因此仍保留为基线。
- #8 不会在当前外键约束下造成已证实的数据悬空，优先级低于其余项目。
- #1～#6 的代码修复和自动化回归已通过 Windows CI 33352139368；真实版本化 S3、系统密钥环故障、目标平台进程生命周期和 Worker/D1 调度竞态仍需故障注入或集成验证，不能仅凭静态检查或 CI 宣称外部边界全部关闭。

### 外部验证顺序（不阻塞当前便签设计）

真实系统密钥环故障、版本化 S3 删除和目标平台进程生命周期仍需按实际测试条件逐项验证，并根据结果更新本节状态；这些外部验收不与当前便签产品设计混合实施。D1 并发与 Worker 身份/工作区隔离只在重新启用远端公开或同步能力时恢复为前置工作。
