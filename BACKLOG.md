# Backlog

本文档只记录主体框架完成后再评估的功能，不代表当前实施范围。

## 智能引用第二层与批处理

- 第一层文本向量候选、本地/远端提供者和阈值自动引用已经移入 `DEVELOPMENT_PLAN.md` 阶段六，不再属于 backlog。
- 只有实际使用证明纯向量候选的排序质量不足时，才评估小型重排模型；不得仅因为模型存在就增加部署体积和推理耗时。
- 只有需要理解否定、时间条件、复杂规则或生成解释时，才评估本地或远端 LLM。LLM 必须消费第一层缩小后的少量候选，不能扫描整个资料库替代向量召回。
- 后台批量分析、定时重新索引、批量接受或撤销自动引用，需要先补齐任务进度、取消、失败恢复、费用上限和自动结果审计，再单独设计。
- 自动建立的引用若将来需要按模型批量撤销，必须先增加独立的本地审计记录；不得通过修改 `Reference` 核心模型来绑定某个 AI 提供者。
