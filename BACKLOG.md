# Backlog

本文档只记录主体框架完成后再评估的功能，不代表当前实施范围。

## 本地智能引用推荐

- 根据节点名称和内容，在本地从用户指定的候选节点中推荐引用。
- 默认只提供候选结果，由用户确认后创建普通 `Reference`，不静默修改正式数据。
- 优先评估可在普通电脑 CPU 上运行的本地文本向量模型和 ONNX Runtime。
- 模型运行、向量索引和候选集合属于可替换的本地智能适配器，不进入 `Node + Reference` 核心模型，也不依赖 Cloudflare。
- 节点、引用、API、存储、桌面主流程和画布主体完成前不实施。
