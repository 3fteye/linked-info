# Backlog

本文档只记录主体框架完成后再评估的功能，不代表当前实施范围。

## 智能引用后续能力

- 第一层向量与图传播、第二层本地 LLM 复核均已进入 `DEVELOPMENT_PLAN.md` 并完成，不再属于 backlog。
- 只有实际使用证明现有候选排序不足时，才评估小型重排模型；不得仅因为模型存在就增加部署体积和推理耗时。
- “智能引用操作记录与批量撤销”不是拿用户数据训练或微调模型。它只是可能有需求时再实现的设备本地操作记录，保存某次分析使用的算法/模型指纹、建议的候选 ID、最终接受或拒绝的候选，以及由该次分析自动新增的引用。
- 操作记录的第一用途是解释和按一次分析撤销自动修改；第二用途才是用本地统计比较算法版本。任何把这些记录用于训练、上传或跨用户聚合的能力都必须另行设计并由用户明确开启。
- 后台批量分析、定时重新索引、批量接受或撤销自动引用，需要先补齐任务进度、取消、失败恢复、费用上限和上述操作记录，再单独设计。
- 操作记录属于设备本地派生数据，不修改 `Reference` 核心模型；密码保护工作区中也必须加密，默认不导出、不同步。

## 搜索与关系浏览

- 名称/内容搜索、搜索范围切换、画布上下文透明度和多引用 AND 筛选均已完成，不再属于 backlog。
- “谁引用了当前节点”的反向引用浏览已转入当前实施范围，不再留在主体框架完成后的候选列表。
- 保存、命名和复用常用筛选组合尚未形成实际需求；只有手动重复配置筛选成为明确负担时再评估。

## 内容展示与本地处理

- Markdown 安全预览、共享内容处理器宿主、显式内容标记、TOTP 和秘密展示均已完成，不再属于 backlog。
- 代码预览处理器已经转入当前实施范围，不再属于 backlog。第一版使用 `code.<language>` 处理器 ID 支持显式语言/格式选择、语法高亮、固定行号、长行滚动和受控复制；换行与行号偏好只有在实际使用证明固定行为不足后才通过版本化配置扩展。它只改变内容的本地展示和使用方式，不新增代码节点等领域类型，也不执行正文。
- 脚本执行必须最后单独设计权限、隔离、超时、取消、输入和输出边界，不能把节点内容直接交给 WebView 或系统 shell 执行。

## 桌面便签与时间视图

- 桌面便签模式是正式节点的独立视图还是独立暂存记录尚未决定。必须先处理锁屏、休眠、崩溃、强制关机、明文落盘和自动锁定边界；不能依赖“关机时再加密”保证数据安全。
- 时间画布与自动时间节点继续复用正式 `Node + Reference`。后续需要决定日期粒度、合并规则、归档语义以及由核心功能还是受约束官方扩展负责生成，不能把时间关系只写入扩展私有元数据。

## 画布导航

- 多画布、位置添加/移动、按节点跳转和独立位置书签已经进入主体功能，不再属于 backlog。
- 位置书签已明确保存目标画布、视口和唯一名称，并纳入 v5 工作区、导出、历史、恢复和加密备份；不得把布局坐标写入 `Node`、`Reference` 或普通第三方扩展元数据。

## 架构审查后续基线（2026-08-30）

本节记录用户提供的仓库级静态审查（固定基线 `main@1743de8`）经过当前工作树自查后的结论。当前分支已经推进到 `7d44f43`；用户已确认该基线，以下项目已经进入修复。原始审查结论仍保留作为证据，代码是否关闭还要以 Windows CI、故障注入和真实服务验证为准。

### 必须保留

1. **数据密钥轮换的提交后清理（P1）**：已拆成 `CommittedCleanupPending` 状态；vault 提交后旧系统凭据或事务目录清理失败只保留可重试状态，不阻断新 vault 的启动和解锁。已加入故障注入测试，待 Windows CI 验证。
2. **扩展锁定竞态（P1，安全边界）**：托管扩展现在贯穿使用访问许可代数，启动、写入 IPC 和响应返回均复验同一代数；锁定先推进运行时撤销边界再清除 vault，并有 barrier 回归测试。待 Windows CI 验证。
3. **离站备份配置跨介质事务（P1）**：创建、更新、移除现在使用带 HMAC 的持久化事务日志；配置提交和系统密钥环清理可在下次读取时恢复，提交后清理失败返回明确的待重试状态，且不会覆盖未完成日志。待 Windows CI 与密钥环故障注入验证。
4. **加密启用与向量缓存写竞态（P1，安全边界）**：缓存读写在取得锁后再次验证加密状态和缓存代数；加密迁移前后推进代数并清空缓存，迟到写入会被丢弃。已加入 barrier 回归测试，待 Windows CI 验证。
5. **版本化 S3 桶的远端删除语义（P1）**：`BackupTarget` 已增加版本删除/全量清理契约；S3 适配器枚举 `ListObjectVersions` 的对象版本和删除标记，带 `versionId` 删除并复核为空。不能证明完整删除时返回 `Unverified`，桌面端保留目标配置。待真实版本化桶验证。

### 条件性保留

6. **Cloudflare Worker 身份与工作区隔离（远端启用前 P0 发布闸门）**：Worker 当前仍匿名暴露全局节点/引用 CRUD，D1 表没有主体或工作区维度。桌面端目前不调用它，文档已明确禁止直接公开部署，因此暂不引入简单共享 Token；如果保留 Worker 作为公开或同步后端，必须先设计经过验证的主体、工作区作用域、数据库约束和越权集成测试。
7. **D1 创建引用的原子性（P2；仅在继续维护 Worker API 时）**：已改为 D1 原子 batch，在同一事务快照内以 `INSERT ... SELECT ... WHERE EXISTS` 检查端点，并把重复引用与端点不存在区分开。仍缺真实 D1 并发集成测试。

### 已发现的补充边界

- 智能引用结果缓存的 Rust/TypeScript DTO 现在都要求 `sourceFingerprint` 为 64 位小写 SHA-256；旧的缺字段条目按损坏缓存丢弃，不得当作有效结果使用。
- 版本化 S3 的破坏性删除在版本 API 不可用、分页无法验证或应用前缀下出现任何无法解析的对象时返回 `Unverified`；不能因为普通列表过滤了畸形键就报告“已删除”。
- 向量缓存和本地 LLM 使用有界代数与取消复验。代数达到 `u64::MAX` 时拒绝新写入/迁移，不回绕；模型哈希在 `spawn_blocking` 返回后仍需检查取消，取消不得发布校验标记或 `Ready` 状态。
- 数据密钥轮换若已确认正式 vault 已提交，即使提交后清理标记暂时写不进去也放行新 vault；下次恢复继续重试标记、旧凭据和事务目录清理。若无法证明 vault 已提交，仍必须阻断并要求恢复。

### 自查判断

- #1 和 #5 已有文档披露或限制，但文档不能替代代码中的认证、作用域和删除完成语义，因此仍保留为基线。
- #7 不会在当前外键约束下造成已证实的数据悬空，优先级低于其余项目。
- 以上项目的代码修复已写入当前工作树，但真实版本化 S3、密钥环故障、Windows 进程生命周期和 Worker/D1 调度竞态仍需 CI、故障注入或集成验证，不能仅凭静态检查宣称全部关闭。

### 建议修复顺序

代码层面的审查修复已完成当前切片；接下来只做 Windows CI、密钥环故障注入、真实版本化 S3 删除和 D1 并发集成验证，并根据结果更新本节状态。Worker 身份与工作区隔离仍是远端公开/同步启用的前置闸门，不应通过低成本共享令牌绕过主体隔离设计。
